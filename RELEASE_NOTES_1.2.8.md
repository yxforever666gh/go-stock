# 1.2.8

`1.2.8` 不是再往界面上硬塞一个大功能，而是把这段时间已经长出来的几个“复杂区”继续拆开，让后续迭代更稳。重点集中在三件事：市场页分栏重构、收益率概览与重算链路整理、运行时和发布工程化补齐。

### 这次为什么要做

- `市场行情` 页长期承载太多互不相同的内容，继续把快讯、指数、龙虎榜、公告、研报、热门题材都堆在一个超大组件里，维护成本已经明显高于新增功能本身。
- `收益率统计` 页虽然已经独立出来，但图表和概览计算如果继续从明细列表侧拼装，后续再补 cohort、缓存或更多图表口径时，容易和明细链路重新缠在一起。
- 应用层运行时状态、任务互斥、菜单切换事件和发布校验如果继续散在各处，后续无论是桌面模式还是 Web 模式，排查都会越来越费劲。

### 本版重点变化

- `市场行情` 页拆成更清晰的分栏结构：
  - `市场快讯`
  - `全球股指`
  - `重大指数`
  - `行业排名`
  - `个股资金流向`
  - `龙虎榜`
  - `个股研报`
  - `公司公告`
  - `行业研究`
  - `当前热门`
  - `指标选股`
  - `名站优选`
- 顶层菜单与运行时事件进一步模块化：
  - 菜单配置从 `App.vue` 中抽离
  - 市场页和研究页切换统一走事件入口
  - 前端运行时错误与加载事件集中注册/清理
- `收益率统计` 页现在有独立的按交易日 overview 数据链路：
  - 支持统一生成全库累计走势与单日变化数据
  - 查询结果会按关键表更新时间做缓存签名
  - 统计页后续补口径或图表时不必再回头改明细列表主逻辑
- AI 推荐收益率重算代码继续拆分：
  - 激活判定
  - 提供方选择
  - 重算调度
  - 概览统计
  - 写库入口
  这些职责已从原先的大文件里拆开，后续改动更容易做定向测试。
- 应用运行时状态收口：
  - cron entry 管理
  - 总结任务互斥
  - 收益邮件任务互斥
  - DOM ready 一次性状态
  都统一到了更明确的辅助层。

### 对使用体验的影响

- 市场页的栏目切换和后续扩展会更稳定，不容易因为改一个 tab 影响整页其它功能。
- 收益率统计页和收益率明细页的职责会继续保持分离，后续新增图表或统计口径时更不容易把明细页再次拖回“大杂烩”。
- 启动、定时任务和邮件任务这类运行时流程更容易维持一致性，后续排查“为什么这次没触发 / 为什么重复触发”时也更直接。

### 本次同时更新的发布物

- `CHANGELOG.md`
- `RELEASE_NOTES_1.2.8.md`
- `README.md`
- `wails.json`
- About 页默认版本说明
- 前端 `package.json` / `package-lock.json`

### 验证结果

- `go test ./...` 已通过
- `cd frontend && npm run build` 已通过
