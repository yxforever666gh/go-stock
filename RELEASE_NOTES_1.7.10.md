# Go-Stock 1.7.10 发布说明

1.7.10 将股票推荐记录中的“买入金额、卖出金额、当前金额”改为“买入价、卖出价、当前价”。三列现在展示每股价格，不再展示整个仓位的资金金额。

买入价和卖出价按实际模拟成交数量计算加权成交均价；当前价来自未平仓仓位的最新行情。尚未买入、尚未卖出或已经平仓后不适用的价格显示为 `--`。

原有净金额字段继续保留用于 API 兼容和净收益率计算，同时新增明确的 `buyPrice`、`sellPrice`、`currentPrice` 字段。本版本不修改数据库结构，主库保持 schema 12，分钟库保持 schema 2。
