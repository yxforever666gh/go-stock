# 更新日志

## 1.7.4 - 2026-08-21

- 推荐详情的分时与1分钟K按交易日增加竖向虚线，价格区和成交量区同步分隔。
- 删除最后分钟柱重复的“最新”圆点；新增全局价格横线开关，统一控制最新价、买入均价和卖出均价。
- 价格横线默认隐藏，选择保存在当前浏览器并对全部推荐详情生效。
- 主库继续使用 schema 11，分钟库继续使用 schema 2，不执行数据库迁移。

## 1.7.3 - 2026-08-20

- 领域 HTTP handler 直接依赖窄服务接口，删除透明 service 转发层和单一 God adapter，降低跨层修改成本。
- 引入统一运行时协调器，后台任务支持取消和有界等待；研究读请求继续传递 HTTP 上下文。
- 前端统一轮询与拖拽生命周期，研究 Client 开始使用生成契约类型；network-audit 移出主程序模块。
- Windows 与 Linux 启动、重启、发布脚本统一为 Web-only，不再传递或识别废弃的 `--web` 参数。
- 主库继续使用 schema 11，分钟库继续使用 schema 2，不执行数据库迁移。

## 1.7.2 - 2026-08-20

- 设置页收敛为通用、数据接口、AI 分析和配置管理四个领域，分钟来源与 AI 模型均可排序和独立启停。
- 自动分析开关只控制定时触发，手动研究与持仓生命周期继续使用同一研究后端。
- 移除普通诊股、Prompt 模板及其专用 HTTP/导出入口，市场行情、股票自选、基金与研究中心保持不变。
- 主库升级到 schema 11，增加分钟来源排序和持仓复查时间配置；分钟库保持 schema 2。

## 1.7.1 - 2026-08-19

- 新增幂等的历史资金与买入纠正命令，按明确行情来源补记指定的两笔错过成交。
- 前两期计划入金可按历史有效时间登记，单位净值和 TWR 不受外部资金流影响。
- 历史纠正保留原事件并追加审计时间线，后续卖出判断从指定交易日 09:50 重新锚定。

## 1.7.0 - 2026-08-19

- 模拟账户从升级后的下一个交易日起分四次追加资金，累计净入金上限为 50 万元；所有入金使用独立审计流水且不计入策略利润。
- 账户收益改为剔除外部资金影响的时间加权收益率（TWR），并增加净值快照、最大回撤、胜率、盈亏比、费用、换手率和资金利用率等评估指标。
- AI 推荐容量从“任一持仓即阻断”改为最多 10 个持仓/待买敞口，每个待买任务预留 5 万元且每轮仍最多推荐 2 只。
- 持仓复查保持每只股票上下文隔离，卖出判断最多 5 路并发；共享市场数据按 15 分钟桶去重，慢模型不再阻塞全部持仓。
- 主库升级到 schema 10，分钟库保持 schema 2；现有现金、持仓、交易和历史收益记录原样保留。

## 1.6.9 - 2026-08-19

- 分钟图数据源模式改为优先级：私人源、腾讯、新浪与 AKShare 可组成自动回退链，不再互斥。
- 当前来源报错、返回空数据、数据不合规或覆盖不完整时自动尝试下一个已启用来源，完整覆盖后停止。
- 研究中心金额、价格、数量、费用、成交量和净收益统一采用三位逗号分隔，并限制合理小数位。
- 设置页显示实际回退顺序和每个来源开关；主库继续使用 schema 9，分钟库继续使用 schema 2。

## 1.6.8 - 2026-08-19

- 股票推荐与收益详情新增完整持仓期分时/一分钟 K 线、成交量、十字光标、买卖点和全成本净收益。
- 分钟图表优先读取本地缓存，只在打开详情和人工刷新时补齐数据；缺失交易日如实展示。
- 全站 Markdown 预览统一为类 ChatGPT 的左对齐阅读排版，长表格独立横向滚动。
- 主库继续使用 schema 9，分钟库继续使用 schema 2；不执行数据库迁移。

## 1.6.7 - 2026-08-18

- 正式收敛为本机 Web-only 架构，删除 Wails 桌面运行时、生成绑定、打包配置和双前端桥。
- 以显式 `/api/v1` typed HTTP API 替换 72 个反射 RPC，按系统、设置、分组、股票、基金、市场、AI、研究和导出领域拆分。
- 前端统一使用领域 API Client 和浏览器事件/WebSocket 运行时；市场行情 12 个页签及全部现役功能保持不变。
- 主库继续使用 schema 9，分钟库继续使用 schema 2；不执行数据库迁移。

## 1.6.6 - 2026-08-18

- 将 1.5 旧策略、旧主库分钟缓存、收益状态和回测表制作永久完整归档后移出活动主库。
- 新增离线主库压缩、归档校验及数据库联动回滚，schema 9 失败时恢复 1.6.5 与 schema 8。
- 保留市场行情、股票自选、普通诊股、基金、研究中心和完整情感历史；分钟库保持 schema 2。

## 1.6.5 - 2026-08-18

- 删除新推荐的等待激活流程，AI 推荐通过交易校验后按最新行情直接模拟买入；午休或盘后完成时顺延到下一有效交易时段。
- 买入只尝试一次，不可交易和资金不足分别形成明确终态；账户、持仓、交易和买入事件使用同一事务落库。
- 卖出判断从下一交易日 09:50 开始，并固定在上午 15 分钟序列及午后 13:00 起的 15 分钟序列，不因模型耗时漂移。
- 研究中心删除新流程的激活条件和激活窗口展示，历史激活字段与时间线只在旧记录详情中保留。
- 主库 schema 升至 8，迁移旧待激活记录进入一次性待买队列；分钟库保持 schema 2。

## 1.6.4 - 2026-08-18

- 生命周期每 15 分钟采集实时行情、分钟量价、指数/行业和增量事件，并按激活条件定向补充资金、公告和互动来源。
- 实时行情或分钟量价失败时不调用 AI；未激活阶段每轮抵扣 15 分钟、累计最多抵扣 30 分钟。
- 激活和卖出必须引用本轮实时行情与分钟量价来源，推荐详情可折叠查看每轮证据快照与来源状态。
- 主库 schema 升至 7，分钟库保持 schema 2；现有推荐、独立会话、交易和收益历史保持不变。

## 1.6.3 - 2026-08-17

- 自动分析仅在沪深交易日开盘时段启动；休市时不调用模型，也不创建跳过或失败报告。
- 人工分析不受日期和时间限制，盘后完成的推荐从下一交易日继续判断。
- 未激活推荐统一按累计 4 小时真实开盘交易时长计时，午休、盘后、周末和休市日不消耗时长。
- 恢复 2026-08-17 盘后生成的中际旭创和中微公司推荐，保留原失效事件并追加纠正记录。
- 研究中心默认打开并优先展示“股票推荐记录”；主库 schema 升至 6，分钟库保持 schema 2。

## 1.6.2 - 2026-08-17

- 研究分析与生命周期判断改为流式模型调用，活跃推理持续等待，仅连续 300 秒无有效事件才超时。
- 瞬态故障在当前模型最多尝试 5 次；鉴权、参数和协议不兼容等不可恢复错误立即顺延下一模型。
- 研究报告详情新增结构化模型调用记录，展示阶段、次数、状态、最后活动、错误分类和后续动作。
- 取消后台分析与生命周期的固定整轮超时；CLI 默认无限等待并保留可选人工超时。
- 主库 schema 升至 5，分钟库继续使用 schema 2，历史记录保持不变。

## 1.6.1 - 2026-08-15

- AI 模型配置表改为唯一回退顺序，并为每个模型增加独立启用开关。
- 研究分析和生命周期判断统一为单次 30 秒、每模型最多 5 次，连续失败后再切换下一模型。
- 研究中心新增手动“开始 AI 分析”按钮；运行期间禁用，报告生成后恢复。
- 修复 Web RPC 对 `(结果, nil)` 的解包、分析列表负载和 Windows 启动脚本识别。
- 主库 schema 升至 4，保留全部历史表与研究记录。

## 1.6.0 - 2026-08-14

### 全新 AI 研究流程

- 删除 1.5.x 策略、cohort、冻结回放、runtime guard、preflight、策略审计、兼容链路和独立 AI 智能体。
- 新增大盘、板块、个股、决策四级 AI 分析，整合现有资讯、行情、研报、公告、财务、概念和资金数据。
- 每轮最多入库两只可交易沪深 A 股并允许空仓；最终报告强制包含固定推荐表格。
- 每只股票使用完全隔离的生命周期会话，每 15 分钟由 AI 判断激活、失效、持有或卖出。

### 模拟交易与收益

- 新增 10 万元全局模拟账户、5 万元单股上限、整手规则、T+1、涨跌停和停牌成交约束。
- 统一计入佣金、最低佣金、卖出印花税、过户费及双边滑点。
- 仅保留净收益额和净收益率；删除基准收益率、超额收益率、XIRR 和旧收益重算。

### 数据与界面

- 主库升级到 schema 3，新增独立 `research_v160_*` 表；旧表和历史行保留但运行代码彻底断开。
- 删除旧策略 guard/immutable 触发器，分钟库继续使用 schema 2。
- 研究中心仅保留 AI 分析报告、股票推荐记录、股票收益率三个页签。
- 设置页新增 AI 分析开关、配置和自动分析时间；市场行情保持为独立读取模块。
- 发布身份收敛为 App `1.6.0` 与数据库版本，不再暴露策略版本和配置 hash。
