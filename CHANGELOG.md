# 更新日志

## 未发布

### 改进

- 设置页 AI 模型服务配置改为拖拽排序：不再手动填写“序号”，可直接拖动左侧排序手柄调整大模型优先级；新增、删除、拖动后会自动重排 `sort` 并沿用现有自动保存逻辑持久化。
- 手动下载分钟线改为缺口闭环：跨日/上一交易日缺口优先走历史分钟线源，缺口下载与完整性判断按交易时段覆盖而非自然时间窗口判定，多轮重试仍失败时落库为“不可覆盖”并展示明确原因，避免任务结束后长期停留在“待覆盖”。

## 1.4.1 - 2026-06-26

### 重点更新

- 新增策略阶段 `V1.4.1`，`current` 默认切到 `summary_version = 1.4.1`，继续沿用现有 `conditional / analysis_only / activation_status` 语义和生产候选最多 4 只上限。
- 市场总结推荐补齐二轮 AI 补位闭环：第一轮保存后若生产候选不足 2 只，会从剩余 verified candidates 中最多触发一次补位表格生成，并继续走同一套保存前硬规则与激活门控。
- 市场总结正文末尾追加“补位说明”，记录是否触发补位、补位候选和仍被拦截的主要原因，不改写原有 7 个一级标题主体结构。
- 新增市场总结运行诊断表，落库候选池数量、AI 输出数量、保存数量、生产/仅分析数量、拦截原因 Top 5、空转状态和运行备注。
- 推荐收益统计页新增“市场总结诊断”面板，支持随策略阶段筛选查看候选池、AI 输出、保存结果、生产候选、仅分析和最近连续空转次数。
- 候选池评分增强加入板块强度、近期失败记录和数据完整性因子，并输出 `scoreBreakdown`，评分只影响排序和送 AI 优先级，不替代硬规则。
- 前端推荐记录、收益列表和收益统计筛选入口新增 `V1.4.1`，后端 `normalizeStrategyCohort` 支持 `1.4.1 / v1.4.1 / 141 / v141`。

## 1.4.0 - 2026-06-25

### 重点更新

- 新增策略阶段 `V1.4.0`，市场总结新推荐默认写入 `summary_version = 1.4.0`，继续沿用现有 `conditional / analysis_only / activation_status` 状态语义。
- 市场总结推荐前置候选池生成：复用东财指标选股能力，固定运行强势突破、量能确认、均线多头、回踩支撑和板块领涨 5 组模板，合并去重后形成临时候选池。
- 候选池增加轻量评分与补位：按涨幅、量比、换手、成交额和价格区间排序，AI discovery 结果不足时自动从指标候选池顺延补位。
- AI 最终推荐队列从最多 6 只扩展到 12 只，要求按优先级输出顺延补位交易计划；生产候选仍最多 4 只，保存与激活硬规则不放松。
- 运行日志补充指标候选池数量、合并后 discovery 候选数量和验证候选数量，便于判断空转来自候选池、AI 质量还是硬规则拦截。

## 1.3.6 - 2026-06-05

### 重点更新

- 新增策略阶段 `V1.3.6`，市场总结新推荐默认写入 `summary_version = 1.3.6`，前端收益统计、收益列表和推荐记录页支持按 V1.3.6 筛选。
- AI 推荐源头增加质量门槛：候选必须具备足够证据类别、高信任证据、有效价格锚点、技术/资金确认和当前窗口新证据，负面信号过多或总分不足会直接丢弃。
- 保存前增加 V1.3.6 交易计划校验：按最差可成交价检查盈亏比、止损空间和关键价位偏离，未通过的记录降级为仅分析并写入具体原因。
- V1.3.6 激活门控改为硬风控加强弱二次确认：盈亏比低于硬底线直接跳过，灰区需要 VWAP 确认；连续两次止损才触发冷却。
- 历史市场总结修复按原记录版本解析旧报告，避免 V1.3.6 新门槛误伤旧版本记录的止盈止损恢复。

## 1.3.5 - 2026-06-02

### 重点更新

- 分钟线缓存从主库 `data/stock.db` 拆分到独立行情库 `data/minute.db`，新下载分钟线默认只写新库，避免全量下载继续推高主库体积。
- 新增瘦表 `minute_bar`，使用 `(stock_code, trade_time)` 主键和 Unix milliseconds 时间戳，减少旧表 `id / created_at / deleted_at` 等字段带来的额外体积。
- 分钟线读取改为优先查 `minute.db`，新库无命中时回退旧表 `ai_recommend_minute_bar`，保留历史数据兼容，不改变 AI 分析和 strict 回测口径。
- 新增源码迁移入口 `go run . migrate-minute-db`，可将旧分钟线表批量迁移到 `minute.db`，迁移过程幂等且不会删除旧表。
- 新增分钟线存储单元测试，覆盖默认只写新库、旧表 fallback、临时 dual write、删除清理和迁移幂等。

## 1.3.2 - 2026-05-25

### 重点更新

- 新增独立策略阶段 `V1.3.2`，后续市场总结推荐写入 `summary_version = v1.3.2`；历史 `V1.3.1 / phase3-v4` 数据不回写、不重算，便于分阶段对比。
- V1.3.2 启用三件套硬规则：强弱过滤、盈亏比准入、重复止损冷却；不满足条件的推荐会跳过激活并记录明确原因。
- 收益率相关页面默认查看 `All`，分层下拉改为 `V1.3.2 / V1.3.1 / Phase3-v3 / Legacy`，不再展示 `Current`。
- `收益率统计` 页新增 V1.3.2 硬规则拦截诊断，展示总拦截数以及强弱、盈亏比、冷却三类拦截数量。
- 版本元数据、README 与前端版本号同步更新到 `1.3.2`。

## 1.3.0 - 2026-04-22

### 重点更新

- **回测算法核心优化**：针对 AI 推荐激活规则和收益率统计的五大硬伤进行系统性修复，消除事后拟合、幸存者偏差、固定量能阈值等问题，提升回测结果的可信度。

### 新增

- **P0-1: 防止事后拟合**
  - 激活规则新增时间戳验证机制（`GeneratedAt`、`DataCutoffTime`）
  - 自动检测规则是否使用未来数据，防止"用结果反推规则"
  - 新增历史数据验证统计（`HistoricalValidation`），记录规则在历史数据上的表现
  - 新增文件：`ai_recommend_activation_rule_validation.go`、`ai_recommend_activation_rule_backtest.go`

- **P0-2: 消除幸存者偏差**
  - 新增综合统计结构体 `AiRecommendYieldComprehensiveStats`，统计所有推荐（包括未激活的）
  - 计算激活率、各状态分布（待激活/已跳过/不符合条件）
  - 新增机会成本分析，计算被跳过推荐的假设收益
  - 提供选择偏差调整后的收益率
  - 新增文件：`ai_recommend_yield_opportunity_cost.go`、`ai_recommend_yield_comprehensive_stats.go`

- **P1-1: 量能条件优化**
  - 激活规则从固定"成交额>1亿"改为历史分位数模式（默认近30日70%分位）
  - 自动适应不同市场环境和个股流动性特征
  - 新规则默认使用分位数模式，旧规则保持向后兼容
  - 新增文件：`ai_recommend_activation_volume_percentile.go`

- **P1-2: 基准对比优化**
  - 修复空仓期按0%收益计算的不公平假设
  - 空仓期现在按年化2.5%的货币基金收益计算
  - 基准对比更真实反映"持续投入策略"的实际表现
  - 新增资金利用率计算模块 `AiRecommendYieldCapitalEfficiency`
  - 新增文件：`ai_recommend_yield_capital_efficiency.go`

### 修复

- 调整 AI 推荐激活逻辑中 `sameDayOnly` 和开盘复核的执行顺序：开盘复核（风险保护）现在优先于 `sameDayOnly`（时效限制）执行，确保隔夜推荐在次日开盘时能正确应用跳空止损和追高限制策略，而不是被 `sameDayOnly` 提前拦截。跳过原因现在更准确地反映实际情况（"跳空止损" / "追高保护" / "时效限制"），便于策略优化和问题排查。

## 1.2.8 - 2026-04-21

### 重点更新

- `市场行情` 页完成新一轮分栏重构：原先集中在单文件里的多块内容已拆成独立 tab 组件，市场快讯、全球股指、重大指数、行业排名、资金流向、龙虎榜、公告、研报、热门题材、指标选股和名站优选等栏目切换更清晰，后续继续维护单个栏目时不再需要反复挤进同一个巨型页面。
- 顶层壳层事件与菜单配置从 `App.vue` 中进一步抽离，新增统一的运行时事件注册和菜单配置模块；市场页、研究中心页的切换事件现在走同一套入口，减少“页面逻辑、菜单逻辑、运行时事件”继续糊在一起带来的耦合。
- `收益率统计` 页补齐独立的“全库按交易日收益概览”链路，新增专门的日级 overview 数据构建与缓存逻辑；统计页不再只依赖明细列表拼装图表，后续维护累计走势、单日变化与策略 cohort 口径时更容易保持一致。
- AI 推荐收益率重算主链路继续拆分：激活判断、分钟线提供方、重算调度、概览统计和写库逻辑已从原先的超大文件中分离，排查“为什么没激活 / 为什么没回算 / 为什么统计图不对”时不再需要在单个数千行文件里来回跳。
- 应用运行时状态进一步收口到独立辅助模块：cron entry 管理、总结任务互斥、收益邮件任务互斥和 DOM ready 状态都改成统一封装，减少启动期和定时任务期的并发状态散落在 `app.go` 里的问题。
- 发布与工程化准备同步补齐：新增 GitHub Actions `CI` 工作流，前端构建继续升级到当前依赖组合，About 页默认版本说明、README、更新日志、Release Notes、`wails.json` 与前端版本号同步更新到 `1.2.8`。

## 1.2.7 - 2026-04-20

### 重点更新

- `股票收益率` 页继续收口成明细/排障页：顶部不再重复展示总收益率、沪深300、超额收益、XIRR、回撤、跑赢占比和全库走势入口；这些策略级统计现在只保留在独立的 `收益率统计` 栏目，避免明细页和统计页再次混成一页。
- 修复收益率统计“只有总净收益率有值，其他几乎全是 `--`”的问题：策略自身可计算的 `策略 XIRR` 与 `最大回撤` 已从沪深300基准链路里拆开；即使 benchmark 日线/现金流匹配暂时失败，也不会再把策略侧统计一起清空。
- `研究中心` 新增独立栏目 `收益率统计`，把收益率评估从原先的大明细页里拆成单独的统计视图，便于先看策略整体表现，再回到明细页排查单票结果。
- 新栏目集中展示 `策略收益率`、`沪深300（现金流匹配）`、`超额收益率`、`策略 XIRR`、`基准 XIRR`、`超额 XIRR`、`最大回撤`、`跑赢基准占比`、`超额收益中位数` 等核心指标，不再只能从列表顶部零散读取。
- `收益率统计` 页内直接内嵌“全库收益走势”可视化，支持切换 `累计走势` 与 `单日变化`，不用再先进入明细页再点弹窗。
- 新统计页继续复用 strict 口径与 `沪深300（现金流匹配） benchmark`，更适合和“分批买入、持续投入”的策略做同口径比较，减少直接拿单点买入指数做比较带来的偏差。
- 原 `股票收益率` 页保留个股明细、分钟回放、手动下载分钟线、报错日志和回算操作；统计与明细的职责进一步拆开，连续排查时不再互相挤占页面空间。
- 修复“最近一次手动任务”旧记录误显示为 `预取 -- / 回算 -- / 总耗时 -- / busy 0` 的问题：收益率列表接口现在会对当前运行库补做 `ai_recommend_yield_meta` schema 兜底，旧版本只记录开始时间的任务会明确显示为“历史版本未记录详细耗时”，执行中的新任务则显示为“详细审计待任务完成后写入”。
- `收益率统计` 页为核心指标补上鼠标悬浮解释，包含策略收益率、现金流匹配基准、超额收益率、XIRR、最大回撤、跑赢基准占比、超额收益中位数和数据时间，方便直接在页面里理解这些指标到底在回答什么问题。
- About 页默认版本说明、`wails.json` 版本号、更新日志和 Release Notes 同步更新到 `1.2.7`。

## 1.2.6 - 2026-04-14

### 重点更新

- 股票收益率页新增“查看全库收益走势”弹窗入口，改为小窗展示“累计走势 / 单日变化”两张图，不再占用主页面大表格区域。
- 全库收益弹窗新增按交易日金额变化图，并补齐 `strict` 口径的全库日级汇总接口；卖出后收益冻结、持仓用最新价格覆盖最后交易日，交易日继续按上证指数窗口对齐。
- 单日收益率口径改为按“当日参与盈亏计算的持仓成本”计算，不再用历史全量累计成本池摊薄；区间首日如果当天已有真实盈亏，也会显示真实单日金额和单日收益率。
- 修复股票收益率页顶部总净收益率与全库收益弹窗最后一个累计点不一致的问题；两者现在统一基于同一套 strict 明细、同一批纳入记录计算。
- Web 模式补齐 `/build/appicon.png` 与 `/favicon.ico` 静态路由，浏览器新会话下不再因为图标 404 干扰控制台验证。
- `研究中心 -> AI分析报告` 页继续显示 `Provider / 模型` 列；从 `1.2.6` 起，新生成的市场资讯 AI 报告会稳定写入 `provider_name`，不再只显示 `gpt-5.4 / gpt-5.4-medium` 这类模型名。
- 历史 `AI分析报告` 记录的空 `provider_name` 本次不做数据库回填；旧记录继续按原样显示模型名，新记录按 `provider / model` 展示。
- 私有分钟线 `diemeng` 链路新增“按代理环境自动切 host”逻辑：命中 `mohomoparty` 系统代理时保留原配置；国内直连时会把 `mg.diemeng.chat / diemeng.chat` 自动切到 `data.diemeng.chat`，避免证书域名不匹配。
- `diemeng` 的真实分钟线请求、自检和 `network-audit` 展示统一改为同一套有效地址；当前无代理环境下，直连 `data.diemeng.chat` 可正常返回 `calendar / stock/history`。
- `cls_telegraph_api`、`cls_telegraph_web`、`sina_live_news` 三条资讯链路补上“直连 / 设置代理”竞速抓取；有代理和无代理环境下都会自动选更快成功的路径，不再只能依赖单一路径。
- 财联社 JSON、财联社网页、新浪直播快讯的解析逻辑全部改为健壮模式，字段缺失、结构变动或空列表时只返回空结果或普通错误，不再出现 `nil pointer dereference` 这类 panic。
- `network-audit` 对上述三条资讯源新增 `networkPath / fallbackUsed` 元数据，后续可以直接看出结果来自直连还是代理，排查“网络问题 / 解析问题”时不再混在一起。
- 针对本轮网络链路修复，补充了 `diemeng` host 选择、settings 代理客户端和资讯抓取 metadata 的定向测试，并确认 `backend/data`、`internal/cli` 与全仓空编译通过。
- 股票收益率页里的“当前价格”口径继续收敛为最新行情快照/最近收盘价；strict 列表不再把推荐生成时保存的历史 `stock_current_price` 回退显示成“当前价格”，避免同一只股票在不同推荐记录里出现历史快照价与最新价混用。
- 市场资讯历史激活规则修复流程新增“脏规则恢复”分支：遇到旧记录里 `pullback` 路径被写坏、错误 breakout 阈值触发“偏离过大”降级时，会优先从原始 `BuySignal / Remarks` 重建双路径规则，而不是直接沿用坏 JSON 继续判废。
- 新增 CLI 命令 `repair-market-summary`，可重复执行市场资讯历史激活规则修复；本轮已对当前库执行一次，扫描 23 条，降级 7 条，重建/升级规则 11 条，其中宁德时代 `id=260` 已从错误的 `breakout=20` 规则恢复为 `pullback=398.8-401.2 / breakout=404.8`。
- 严格回算落库后会同步把记录级 `activation_status` 回写到 `ai_recommend_stocks` 原表，`股票推荐记录`、开盘复核等仍读取原表的页面/流程不再长期停留在旧的 `pending` 状态；本轮已把宁德时代 `id=260/267` 的原表状态同步为 `activated`。
- 修复 strict `dirty` 标记长期残留的问题：终态记录（`invalid / skipped / ineligible`）和 `analysis_only` 记录不再被强行覆盖成“待回算”；回算写入终态记录级状态后，也会自动清理对应 `dirty`，避免页面重启后仍反复显示“待回算”。
- 手动下载分钟线流程补上最近一次任务审计：收益率页现在会直接显示最近一次手动任务的开始/结束时间、scope 数、预取耗时、回算耗时、总耗时、SQLite busy 次数和分钟线来源分布，排查“为什么这次又慢了”时不再只能翻日志。
- 手动下载分钟线的 scope 改为“先筛 dirty 里的可执行记录，再兜底到分钟线覆盖仍为待覆盖的股票代码”；`analysis_only`、已终态 `invalid / skipped / ineligible` 等记录不再继续进入手动下载与严格回算，减少无效下载和无意义回算。
- 下载阶段进度写入改为节流刷新，并给收益率快照写入、记录状态回写、进度更新等高频 SQLite 写操作统一加上 busy 重试；在分钟线预取和 strict 回算并发阶段，`database is locked` 更不容易把整轮手动任务拖慢或打断。

## 1.2.5 - 2026-04-07

### 重点更新

- 市场资讯 AI 推荐新增 `activation_rule_json` 结构化激活规则，收益率 strict 模式改为只读取机器规则，不再从 `Remarks / BuySignalDetail / Markdown` 自然语言里猜触发条件。
- 修复“AI 报告已生成但股票收益率没落库”的主链路问题：市场资讯推荐补写改为逐条保存，单条推荐因模糊触发词或规则不完整失败时，不再拖死整批推荐入库。
- 推荐记录保存校验升级为“执行字段零模糊容忍”：`放量 / 缩量 / 强势 / 承接 / 不追` 等词不能直接充当激活条件，必须转换成价位、周期、基准、阈值都明确的结构化规则。
- 收益率 strict 口径继续收紧为“只认结构化激活规则”：未结构化记录会明确显示为“未结构化”，并带上失败原因，不再混入正式收益率统计。
- 兼容层不再让 `Remarks` 反向污染买入激活判断：买入区解析、上一交易日活跃度约束和 strict 触发扫描不再把备注文本当成机器信号来源。
- 市场资讯 prompt、AI 工具参数 schema 与保存接口同步升级，后续模型在创建推荐时必须传入 `activationRuleJson`，从源头减少模糊词重新流入。
- 市场资讯 AI 总结定时任务默认时间调整为 `09:40 / 11:30 / 14:30`；其中 `09:40` 执行后会自动生成“开盘复核”结论，补写到市场总结与推荐收益展示链路里，推荐列表、收益率列表和分钟回放弹窗都能直接查看晨间复核动作与原因。
- 市场资讯邮件链路改为围绕“总结任务”触发：支持在市场资讯页手动发送当前总结邮件，定时总结完成后也可按设置自动发送；当 AI 总结失败但有错误原因时，邮件侧同样可以把失败原因发出去，便于远程排障。
- 设置页继续收敛到当前真实流程：新增“定时 AI 总结后自动发邮件”开关，移除独立的收益率邮件定时发送入口，并提供“修复历史 AI 总结备注”兼容工具，把旧报告或旧备注里的结构化规则转成人类可读文案。
- 研究报告、推荐记录、收益率三处列表统一使用共享日期范围状态，跨页面切换时不再各自回到不同默认时间段，连续排查同一批推荐时上下文更连贯。
- 设置页交互改为自动保存，常用开关、分钟线来源、代理、邮件与 AI 配置在修改后会即时落库；AI 配置列表同时支持上移/下移排序，减少频繁手动点“保存设置”的中断感。
- 启动流程补上历史“已跳过/已放弃”脏状态修复与相关重算排队，旧数据在升级后更容易恢复到和当前 strict 口径一致的展示结果。
- About 页、README、CHANGELOG 与 Release Notes 同步切到 `1.2.5`，把本次“量化激活规则改造 + 落库稳定性修复 + 开盘复核/邮件链路补强”作为正式版本内容对外说明。

说明：`1.2.4` 为基于 `1.2.3` 的全量整理版本，重点补强股票收益率 strict 口径、分钟线回补窗口、上一交易日活跃度校验与上证指数同口径基准稳定性，同时完成应用层 API 拆分、前端路由与页面入口重整、智能体会话体验补强、设置页与分钟线配置收敛，以及发布工程化入口补齐；以下保留 `1.2.3`、`1.2.2`、`1.2.1` 与 `1.2.0` 说明。

## 1.2.4 - 2026-04-05

### 重点更新

- 为公开仓库发布补充一次仓库清理：移除支付二维码、公众号图片、运行数据库、根目录二进制、超大测试库与历史导出汇总文件，同时把 About 页收敛为公开版说明，避免公开快照继续携带个人私域或本地生成内容。
- 自动更新、基础资料同步和可选分享/资讯服务的默认地址改为面向当前公开仓库或显式环境变量控制，不再默认绑定原作者的发布页、私有域名或第三方代理下载链路。
- 应用层接口按领域拆分为 `stock / group / market / market-info / recommend / config / ai / agent / notify / fund` 等独立入口文件，`app.go` 只保留 `App` 主结构、共享状态和通用辅助逻辑，历史上的 `app_common.go` 已移除，后续继续演进时不再需要在单个超大文件里同时维护全部业务入口。
- 前端路由正式收敛到 `frontend/src/views/` 作为页面级入口，`frontend/src/router/router.js` 改为按路由懒加载页面壳；原有主体组件保留在 `frontend/src/components/`，形成“页面装配层 + 业务组件层”的更清晰分层。
- 前端与 Wails 绑定层新增统一出口 `frontend/src/services/app-api.js`，页面和组件不再零散直接引用生成代码，后续替换实现或补充 Web 兼容层时只需要围绕统一服务入口收口。
- 股票页的重型能力进一步下沉到 `frontend/src/composables/useStockHeavyFeatures.js`，AI 智能体会话逻辑下沉到 `frontend/src/composables/useAgentChat.ts`，导出、图表、会话状态和滚动行为不再堆在页面组件主体中，维护成本明显下降。
- AI 智能体页面重做为带历史侧栏的多轮会话体验：支持会话创建、历史列表、标题自动总结、消息持久化、轮次导航与切换，便于在连续分析同一批标的时回看上下文而不是每次从空白窗口重来。
- 分钟线配置链路补上运行时覆盖能力，新增 `internal/config/runtime_overrides.go`，桌面设置、Web 模式和环境变量之间的优先级关系更清晰，便于后续做运行时热更新和测试注入。
- 工程化入口补齐根目录 `Makefile`，新增统一的 `make test / make lint / make build-web / make dev-web / make build-desktop`，降低本地协作和 CI 脚本维护成本。
- `.gitignore` 补充本地工作区、运行时备份和私有集成说明忽略规则，仓库内移除公开的 `docs/integrations/diemeng-api.md`，避免把本地私有接入说明继续暴露到公开历史中。
- 设置页新增“分钟线数据源”分区，分钟线配置不再只停留在环境变量层；现在可以直接在界面里切换“公共源优先 / 私人分钟线来源”并保存为真实运行配置。
- 默认分钟线策略调整为“公共源优先”：AKShare、新浪、腾讯保留为公共开关，东方财富改为 AKShare 内部来源偏好，不再和 AKShare 重复暴露两套入口。
- 长历史分钟线场景补充显式约束：公共源模式下不再隐式回退到私人来源，遇到长历史窗口会直接提示改用私人分钟线来源，避免“看起来在下载、实际一直补不齐”的误判。
- 设置页新增“私人分钟线来源”通用配置入口，支持填写调用 URL、API Key、超时、最小间隔、代理模式和分钟级别；旧 `GO_STOCK_DIEMENG_*` 环境变量只保留兼容与迁移导入用途。
- 基础设置移除“指数基金”和“赞助码”两个入口，减少与当前股票收益率和分钟线主流程无关的干扰项。
- 通知设置收敛为“只保留邮箱推送”，移除钉钉、本地推送、弹幕、滚动快讯和市场资讯提醒等入口，并同步停用对应的桌面提示链路。
- 最近邮件发送日志改为每页 5 条，支持上一页/下一页翻页，便于在发送记录较多时继续追溯失败原因。
- 私人分钟线对接说明改为通用公开模板，不再在仓库中保留任何具体私有服务地址或私有域名默认值。
- 股票收益率页继续收敛到单一 strict 口径：页面查询默认只走严格落库快照，不再混用“快速估算”和“分钟线触发”两套结果，减少调试阶段同一批推荐在不同口径下互相打架。
- AI 推荐买入语义继续压缩到“立刻买入 / 激活买入”两类，收益率统计不再依赖 `低吸 / 观察 / 右侧确认` 等展示标签，避免标签语义干扰实际回测执行口径。
- strict 回测新增“上一交易日同价位活跃度”约束：当买入信号要求当前 5 分钟量能不能弱于上一交易日同时间段时，系统会优先比较成交额，缺失时退化比较成交量；若上一交易日基准缺失，则该推荐不会被误激活。
- 手动下载分钟线在遇到“上一交易日活跃度”约束时，会自动把下载窗口前移到上一交易日对应时段，补齐 strict 回测所需基准分钟线，减少“明明有规则但因为少一天数据而无法回算”的情况。
- 分钟线补齐链路继续强化多源兜底，strict 回测在调试和批量补算阶段更容易拿到完整分钟线，降低大批股票长期停留在“待回算 / 未覆盖”的概率。
- 股票收益率分钟回放窗口扩大为“信号/买点前一交易日 + 区间本体 + 卖点/当前后一交易日”，手动下载分钟线也会同步把前一交易日上下文补进缓存，回放图不再只剩触发当天的孤立分时。
- 股票收益率分钟回放进一步修正持有场景的收口逻辑：非交易时段不再把“当前点”硬延伸到未来交易日，盘后 `current_price_time` 会自动压到最近一根可落图分钟线，减少“明明有数据却提示终点缺口”的误导。
- 回放弹窗里的成交量子图改为压缩显示并提高最小柱高，避免个别异常放量把后续正常量柱全部压扁成“看起来一大片空白”。
- 回放图补上跨日和午休分隔，分钟线在前一交易日、午休、后一交易日之间会显式留出断点，不再把两天数据硬拼成一整条连续曲线，降低“图像星星点点/乱成一坨”的误判。
- 成交量子图进一步改成对数压缩显示，极端放量柱不再把其余分钟全部压成贴地直线；同时保留原始量能 tooltip，避免为了视觉可读性损失实际数值。
- 回放状态文案收口到“核心区间”本身：扩出来的前后交易日上下文不再误报成“分钟线部分缺失”，非交易时段信号点也不再额外弹出近似定位警告，减少把正常盘后信号误看成数据 bug。
- 修复收益率页“上证指数（同口径）”偶发显示 `--` 的问题：同口径基准现在按激活窗口建立缓存，优先使用日线收盘并择机叠加指数实时报价，不再因为单次慢请求或临时超时把整页 benchmark 直接打空。
- 补充 benchmark 相关单元测试与集成测试，覆盖起始交易日归一化、K 线开收盘选择、缓存报价有效性，以及“给定已激活样本时必须返回非 `--` 基准涨跌幅”的真实网络回归场景。

## 1.2.3 - 2026-04-01

### 重点更新

- AI 分析报告、股票推荐记录、股票收益率三条链路统一到“主买入区激活”语义：新增 `signal_time / activation_status / activation_time / activation_price` 字段，未进入主买入区前不再提前计算买入时间、持仓状态和收益率。
- 股票收益率页面改为按激活后再统计：未激活记录统一显示为 `待激活`，总净收益率与上证指数对比也只基于已激活记录计算，避免把“报告生成时刻”误算成买入时刻。
- 股票推荐记录详情弹窗补充“观察价 / 关注位 / 买入区”职责说明，降低 AI 分析报告文案与收益率统计口径之间的割裂。
- 修复收益率重算结果落库不完整的问题：手动下载分钟线或后台重算后，会同步写回 `signal_time / activation_status / activation_time / activation_price`，不再出现“后台已算完但页面全部显示待激活”的假象。
- “手动下载分钟线”操作明确升级为“下载分钟线并触发收益重算”，用于在补齐分钟线后立即刷新激活状态、买入状态和收益率结果。
- 分钟线公共链路补充 Diemeng 兜底：当历史窗口遇到“腾讯超出近 7 天范围”且 AKShare 临时熔断时，会继续尝试 Diemeng 回补关键分钟线，降低“数据完整性未完成 / 股票未激活”的残留概率。
- 股票收益率栏目补充“已跳过 / 已放弃”语义：当 AI 推荐状态为 `avoid / insufficient_evidence / controversial` 或分类为“回避标的”时，不再继续显示为“待激活”，而是明确标记为已跳过且不参与收益率统计。
- 股票收益率栏目新增“激活前自动放弃”规则：若推荐在预期周期内始终未触发主买入区，或在激活前就先跌破止损/失效位，会自动从“待激活”转为“已放弃”，避免长期挂在列表里。
- 修复收益率列表的历史状态覆盖问题：即便数据库里残留旧的 `activated / 持有` 快照，只要 AI 推荐本身已标记为 `insufficient_evidence / avoid / controversial` 或“回避标的”，页面也会强制改显示为“已跳过 / 已放弃”，并同步剔除出收益率统计，避免旧缓存把应放弃的票继续算进收益。
- 统一股票收益率列表“卖出时间”列的颜色规范：`待激活` 保持橙色、`持有` 改为蓝色、`已跳过` 为灰色、`无法回算` 为红色，真正的卖出时间统一使用默认文字色，不再按盈亏结果染成红绿，避免一列里同时混入时间语义和收益语义。
- 股票收益率列表进一步调整展示口径：`买入金额` 在未激活时显示 AI 推荐买入金额并标黄，激活后显示实际买入金额并标绿；`卖出时间` 在待激活时继续标黄，真正卖出后再按收益率着色，盈利为红、亏损为绿。
- 修复收益率列表“已激活但未卖出”记录的卖出时间显示错误：这类记录现在统一显示为“持有”，不再错误落成“待激活”，避免页面上出现“明明已经买入并开始计算收益率，却仍显示待激活”的冲突状态。
- 统一收益率列表“买入金额”和“卖出时间”的颜色口径：两列现在共用同一套状态配色，`待激活` 同为黄色、`持有` 同为蓝色、真正卖出后再按最终收益率红/绿显示，避免同一行里两列颜色语义不一致。
- 修复历史推荐记录的分类污染问题：收益率记录状态表现在会强制把 `recommend_category / recommend_time` 与原始推荐记录对齐，并在升级时自动清洗已有脏数据；推荐记录列表中的空分类也改为明确显示“未标注”，不再把旧记录误渲染成“观察标的 / 低吸候选 / 右侧确认”等后补标签。
- 调整收益率模拟仓位口径：固定测算仓位从 `1000` 元上调到 `3000` 元，降低最低佣金对小幅波动场景的放大效应，并同步更新相关净收益率回归测试。

## 1.2.2 - 2026-03-16

### 重点更新

- 邮件发送链路补充完整审计能力，新增 `email_send_logs`，覆盖测试邮件、收益率 CSV、手动 AI 报告、定时 AI 报告四类发送，并记录收件人、主题、错误信息、附件摘要、报告摘要等。
- 设置页新增“最近邮件发送日志”展示，可直接查看最近发送记录与失败原因，便于排查“没收到邮件”和“是否真的发出”。
- 修复邮件发送重复风险：为手动邮件任务新增互斥锁、为定时 AI 报告新增同一分钟去重与 cron 重载串行保护，避免并发触发或重复注册导致重复发送。
- 收紧收益率 CSV 自动发送条件，移除“页面查询/自动重算完成后顺手发邮件”的旁路，避免项目启动或收益率页面轮询时连续多发邮件。
- SMTP 发送链路补齐真实发送结果判定，不再把未实现或未确认结果视为成功；同时补充 SMTP 直连绑定逻辑，优先绕开 TUN/代理网卡，提升 QQ SMTP 等场景的可用性。
- 市场资讯 AI 总结自动补写推荐记录时，新增“同一天只能推荐不同股票”的限制；同一天已推荐过的股票不再重复入库。
- 清洗历史 `phase3-v2` 市场资讯推荐数据，删除“同一天同一股票”的重复补写记录，仅保留每天每只股票最早的一条。

说明：`1.2.1` 为基于 `1.2.0` 的补丁版本，补充 Codex fallback 与设置页展示调整；以下保留 `1.2.0` 主版本说明。

## 1.2.1 - 2026-03-15

### 重点更新

- Codex 大模型请求新增固定 fallback 链路，按 `RightCode -> codex-for-me -> su8` 自动轮换；仅在网络错误、超时、`429`、`5xx` 等可重试故障时切换，避免把请求参数错误误判为渠道故障。
- 设置页新增 Codex fallback 顺序展示，可直接查看当前三路模型链路，但不再提供“一键导入三路”按钮，避免把机器侧临时凭据隐式写回项目设置。
- 该版本是 `1.2.0` 的补丁发布，其余主功能与使用方式保持不变。

## 1.2.0 - 2026-03-14

### 重点更新

- 市场资讯 AI 总结支持定时执行，新增定时任务、冷却保护、任务运行记录，并在工具模式失败时自动回退到普通总结流程。
- AI 总结结果可自动补写推荐股票记录，减少市场总结与推荐收益统计之间的断层。
- AI 推荐收益率重算链路重构，引入统一的重算管理器，支持全量重算、范围重算、排队重试和运行中状态管理。
- 修复推荐收益率在开盘跳空等场景下的冻结卖出价问题，并补充相应的一致性与回归测试。
- 分钟线抓取逻辑增强，补充抓取窗口控制、提供方策略切换、回退处理和状态健全性校验。
- 新增邮件报告能力，可生成收益率 Excel 与最新 AI 总结 DOCX 附件，并补充收件人、发送时间与 SMTP 配置规范化逻辑。
- 支持邮件直连发送相关配置，默认补齐 QQ SMTP 主机与端口，提升常见个人邮箱场景的可用性。
- 应用层职责拆分，新增配置、文件、自选、分组、基金、通知、提示词、AI 对话、市场服务等独立 service，降低 `app.go` 的耦合度。
- 启动流程与运行时管理进一步整理，补充运行时目录初始化、迁移兼容、panic 保护和 Web / 桌面模式的统一启动辅助逻辑。
- 设置页新增市场资讯 AI 总结、邮件报告等配置项，关于页同步支持展示版本说明。
- 市场页、AI 收益页、个股页与对话页有配套调整，以适配新的推荐收益、定时任务和版本展示逻辑。
- 文档与资源同步更新，包括 `README.md`、推送说明、基础股票数据与运行时相关说明。

### 升级提示

- 从旧版本升级到 `1.2.0` 前，建议先备份 `runtime/db/stock.db` 或 `data/stock.db`。
- 如果使用 Web 模式且页面仍显示旧内容，需要在 `frontend/` 重新执行 `npm run build`，然后重启 `./scripts/start-web.sh`。
- 升级后建议进入设置页检查邮件发送、市场资讯 AI 总结、定时任务与 AI 模型配置，避免沿用旧配置导致新功能未启用。
- 若旧构建未通过构建参数注入 `VersionCommit` 或发布说明，“关于软件”中的版本说明可能为空，但不影响主功能运行。

### 变更范围

- 相对 `1.1.4`，`1.2.0` 共涉及 70+ 个文件调整。
- 主要集中在应用服务拆分、AI 推荐收益计算、邮件报告、市场资讯定时总结、设置页与运行时初始化几个方向。
