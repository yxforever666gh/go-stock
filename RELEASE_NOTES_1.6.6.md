# Go-Stock 1.6.6 发布说明

1.6.6 将已退出运行链路的 1.5 旧策略、收益状态、旧主库分钟缓存和回测表制作永久完整归档后，从活动主库删除并回收空闲页。独立分钟库不压缩；市场行情、股票自选、普通诊股、基金、研究中心和完整情感历史保持不变。

主库升级为 schema 9，分钟库保持 schema 2。升级前会生成包含主库、分钟库、校验清单与 SHA256 的永久 ZIP；旧版本回滚必须同时恢复该归档数据库，不能只切换二进制。

发布流程新增离线主库压缩与数据库联动回滚。归档、SQLite `quick_check`、文件哈希、迁移或启动任一环节失败时，发布不会留下半迁移数据库。
