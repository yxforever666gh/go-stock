# 收益率统计功能使用说明

## 问题诊断结果

### 已修复的问题
1. ✅ **Goroutine 死锁问题**
   - 位置：`backend/data/ai_recommend_yield_overview.go:419`
   - 原因：semaphore channel 容量为 6，当股票代码超过 6 个时新 goroutine 永久阻塞
   - 修复：添加 30 秒超时保护机制

2. ✅ **默认过滤策略过于严格**
   - 位置：`backend/data/ai_recommend_yield_overview.go:20`
   - 原因：默认只显示 `phase3-v4` 版本的记录，过滤掉了大部分历史数据
   - 修复：将默认策略从 `strategyCohortCurrent` 改为 `strategyCohortAll`

### 当前状态
- 服务运行正常（端口 34115）
- 代码修复已完成并编译成功
- **数据库为空**：`data/stock.db` 中的 `ai_recommend_stocks` 表没有任何记录

## 为什么页面显示"无数据"

收益率统计功能需要以下数据才能工作：

1. **AI推荐记录** (`ai_recommend_stocks` 表)
   - 当前状态：0 条记录
   - 需要通过"市场分析"功能生成

2. **分钟线数据** (`ai_recommend_minute_bar` 表)
   - 用于计算推荐股票的价格走势
   - 需要下载后才能计算收益率

3. **收益率状态** (`ai_recommend_yield_state` 表)
   - 后台自动计算生成
   - 依赖于前两项数据

## 使用步骤

### 第一步：生成AI推荐记录
1. 打开应用主界面
2. 进入"市场分析"或"AI推荐"功能
3. 运行市场分析，生成推荐股票记录
4. 确认 `ai_recommend_stocks` 表中有数据

### 第二步：下载分钟线数据
1. 在收益率统计页面点击"下载分钟线"按钮
2. 或者调用 API：
   ```bash
   curl -X POST http://localhost:34115/api/rpc \
     -H "Content-Type: application/json" \
     -d '{"id":1,"method":"StartAiRecommendMinuteDownload","args":[]}'
   ```
3. 等待下载完成（可能需要几分钟）

### 第三步：等待后台计算
- 系统会自动计算每条推荐记录的收益率
- 计算完成后，收益率统计页面会显示数据

### 第四步：查看收益率统计
- 刷新收益率统计页面
- 应该能看到：
  - 总记录数 > 0
  - 收益率走势图
  - 统计指标

## 验证数据

### 检查推荐记录数量
```bash
sqlite3 data/stock.db "SELECT COUNT(*) FROM ai_recommend_stocks WHERE deleted_at IS NULL;"
```

### 检查分钟线数据
```bash
sqlite3 data/stock.db "SELECT COUNT(*) FROM ai_recommend_minute_bar;"
```

### 检查收益率状态
```bash
sqlite3 data/stock.db "SELECT COUNT(*) FROM ai_recommend_yield_state;"
```

### 测试API
```bash
curl -X POST http://localhost:34115/api/rpc \
  -H "Content-Type: application/json" \
  -d '{"id":1,"method":"GetAiRecommendYieldDailyOverview","args":[{}]}' | jq '.result'
```

## 技术细节

### 代码修改
1. **ai_recommend_yield_overview.go:20**
   ```go
   // 修改前
   query.StrategyCohort = normalizeStrategyCohort(query.StrategyCohort, strategyCohortCurrent)

   // 修改后
   query.StrategyCohort = normalizeStrategyCohort(query.StrategyCohort, strategyCohortAll)
   ```

2. **ai_recommend_yield_overview.go:201**
   ```go
   // 修改前
   normalizeStrategyCohort(query.StrategyCohort, strategyCohortCurrent),

   // 修改后
   normalizeStrategyCohort(query.StrategyCohort, strategyCohortAll),
   ```

### 版本过滤逻辑
- `strategyCohortAll`：显示所有版本的推荐记录
- `strategyCohortCurrent`：只显示 `phase3-v4` 版本
- `strategyCohortLegacy`：只显示旧版本（非 `phase3-v4`）

### Web模式API
- 端点：`POST /api/rpc`
- 方法：通过 JSON-RPC 调用 App 方法
- 示例：
  ```json
  {
    "id": 1,
    "method": "GetAiRecommendYieldDailyOverview",
    "args": [{}]
  }
  ```

## 常见问题

### Q: 为什么修复后还是显示"无数据"？
A: 因为数据库是空的，需要先生成AI推荐记录。

### Q: 如何生成推荐记录？
A: 使用应用的"市场分析"功能，系统会自动生成推荐股票。

### Q: 分钟线数据需要多久下载完成？
A: 取决于推荐股票的数量，通常几分钟到十几分钟。

### Q: 收益率什么时候计算？
A: 下载分钟线数据后，系统会自动在后台计算收益率。

## 服务管理

### 启动服务（Web模式）
```bash
GO_STOCK_WEB_ADDR="127.0.0.1:34115" go run . --web
```

### 健康检查
```bash
curl http://localhost:34115/healthz
```

### 查看日志
```bash
tail -f runtime/logs/app.log
```

## 总结

修复已完成，但需要先生成数据才能看到收益率统计。按照上述步骤操作即可。
