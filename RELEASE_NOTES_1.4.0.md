# go-stock 1.4.0 Release Notes

## 版本定位

1.4.0 聚焦“候选池增强 + AI 选股补位 + 交易计划闭环优化”。本次升级不放松硬风控阈值，也不新增生产池、观察池、仅分析池三分法，而是把“谁进入 AI 和硬规则校验”这一步前置做稳。

## 主要变化

- 市场总结推荐默认写入 `summary_version = 1.4.0`。
- 新增指标候选池生成层，复用现有东财指标选股能力，固定运行强势突破、量能确认、均线多头、回踩支撑和板块领涨模板。
- 候选池合并去重后进行轻量评分，评分维度包括涨幅、量比、换手、成交额和价格区间。
- AI discovery 结果会与指标候选池合并，AI 候选不足时自动用指标候选顺延补位。
- 事件发现候选上限从 18 扩展到 36，证据核验上限从 12 扩展到 18，最终推荐股票池上限从 6 扩展到 12。
- 生产候选上限仍保持 4，只是让 AI 多输出按优先级排列的补位交易计划。
- `V1.4.0` 记录继续复用 `V1.3.6` 保存前交易计划硬校验和激活门控，不绕过盈亏比、止损空间、关键价位偏离、VWAP 强弱确认和重复止损冷却。
- 运行日志增加指标候选池数量、合并后 discovery 候选数量和验证候选数量，便于复盘是候选池不足、AI 排序偏弱，还是硬规则拦截过多。

## 风控说明

本版本的目标是减少候选不足导致的空转，不是为了“凑入库数量”放宽规则。若 AI 输出的前几只股票被保存前校验或激活门控拦截，系统仍会保留具体原因，并继续让后续候选有机会进入验证链路；硬规则不过的标的仍不会作为生产交易记录入库。

## 升级后建议观察

- 普通强势行情下，市场总结日志中的 `indicatorCandidateCount` 是否稳定有候选。
- `discoveryCandidateCount` 与 `verifiedCandidateCount` 是否明显高于 1.3.6。
- 推荐记录里被降级为 `analysis_only` 的原因是否集中在同一类硬规则。
- 连续空转时，是指标候选池为空、AI 未输出足够交易计划，还是保存/激活硬门槛集中拦截。
