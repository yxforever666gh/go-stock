# Go-Stock 发布说明

## 2.9.2 - 2026-09-05

- 市场宽度统一红涨绿跌，涨跌类指标移除“只”；市场情绪改为横向四色刻度，指针与文字分离。
- 板块与概念资金流改用完整排行字段，展示主力占比和四档净流入；缺失值显示“—”，零值不加正号。
- 最新交易日使用上游行情时间，新增只读分钟资金时间线，默认比较前三项；刷新和切换时清空旧曲线并忽略过期请求。
- 保留旧资金字段及排序参数兼容，待旧客户端全部迁移到新字段后移除；无新增配置或后台任务。
- 主库保持 schema 26，分钟库保持 schema 3，不迁移或回填历史数据。

## 2.9.1 - 2026-09-05

- 修复研究中心1财务与概念证券代码丢失市场后缀，以及腾讯当前分钟标签令整份分钟证据被误判为未来数据的问题。
- 分钟行情改为安全解析；嵌套空来源明确降级，异常响应不再可能中止持仓复查进程；CLI真实研究入口补齐情感分词初始化。
- 账户现值改用一致数据库快照并标注估值状态；收盘部分快照支持重试升级，调度收敛为15:05—15:55每5分钟一次。
- 中断分析的审计状态自动闭合，失败触发器规范到下一合法交易窗口，各阶段审计记录真实证据截止时间。
- 主库保持 schema 26，分钟库保持 schema 3；不重算或改写历史推荐、成交、持仓和收益。

## 2.9.0 - 2026-09-04

- 退休仅用于 1.7.x 的三套一次性历史纠正命令；1.7.1 missed-cash 记录按原状保留，已执行的 1.7.5/1.7.6 结果和全部审计历史不变。
- 删除应用内更新检查、旧 Linux 运维链、失效 smoke 探针和 GitHub CI，统一使用 Windows 本地验证、发布与部署流程。
- 精简迁移注册、工具模块和重复验证入口，清理无调用代码与会改写工作区的测试副作用。
- 新增 Research 1/2 联合离线验证入口，使用测试 fixture，并以 SHA256 确认生产数据库、WAL 和 SHM 未被修改。

## 2.8.0 - 2026-09-04

- 研究中心2改为工作日09:55启动，使用09:50–09:55最近五根已收盘分钟证据，09:55–13:00启动时可恢复当日任务。
- 选股与执行分别增加距涨停1.5%和1.0%的缓冲，保存执行报价、涨停价、距离及不可成交原因。
- 每轮支持3只主选和3只备选，不可成交时按排名递补；仍不足三笔时通过持久化执行链继续分析至13:00，每日最多三笔。
- 新增主库 schema 26，只增加研究中心2补位链、主备角色和执行审计字段，不重写历史账户、成交或收益。
- 新增项目级复杂度与验证边界，日常维护按 fast/domain/release 分层验证，正式发布只执行一次完整本地门禁。
- Go 包扫描不再进入前端依赖，前端测试改为自动发现；未隔离的真实网络和生产数据库集成入口暂时禁用。

## 2.7.11 - 2026-09-04

- 研究中心1升级到数据链路 `research1-v3`：修正腾讯累计分钟量额及“手/股”均价单位，个股提示改为最新优先的结构化 JSON，并在调用模型前复核行情内部时间。
- 修复 `wait` 被当前价格区间误杀、新分析提前消费旧 wait 和休市跨窗口买入；原始动作、最终动作、决策报价、重分析时间与后继运行均可追溯。
- 研究中心1写入阶段统一串行提交并对 SQLite BUSY/LOCKED/517 完整事务重试；部分机会已持久化后的后续故障显式记为 `partial_success`。
- 主库升级到 schema 25，只追加数据/策略版本、机会动作、报价状态和 wait 后继字段；不重算、删除或改写账户、成交、持仓和收益。分钟库保持 schema 3。

## 2.7.10 - 2026-09-03

- 研究中心2升级到策略 `research2-trailing5-v8`、证据 `research2-trailing5-v6`。午休启动不再按连续墙上时间回退，而是使用上午最后5个已闭合交易分钟 11:25—11:30。
- 修复证据采集成功后在冻结阶段被再次判为空或不可用的问题；正常返回内容保持原有可用状态，失败和降级仍保留完整审计记录。
- 研究中心2报告邮件按 RFC 5322 对超长正文行进行安全折行，避免部分 SMTP 服务拒绝或截断邮件；前端降级提示改为简短摘要，详细来源状态留在证据审计中。
- App 升级到 2.7.10；API结构和数据库 schema 不变，主库保持 schema 24，分钟库保持 schema 3。

## 2.7.9 - 2026-09-03

- 研究中心2的启动窗口统一为交易日 `[09:50,13:00)`；13:00 不再启动新的当日任务。
- 报告在 13:00 前生成时才允许进入模拟执行；13:00 起生成的推荐统一标记为 `analysis_only`，保留研究与审计结果，但不创建成交、不占用资金且不计入收益。
- 新策略 `research2-trailing5-v7` 可对旧策略的失败或空仓结果补跑一次，同版本空仓与既有成功结果不会重复运行；每日最多5次尝试。
- 前端报告、推荐和设置页同步展示上述时间边界。API结构与数据库 schema 不变，主库保持 schema 24，分钟库保持 schema 3。

## 2.7.8 - 2026-09-03

- 研究中心2启动窗口统一为交易日上午 09:50—11:30，并取消 11:30 报告完成硬截止；上午已启动的任务允许跨午完成，不再把可用研究结果记为技术失败。
- 非可执行时段生成的推荐标记为 `analysis_only`（仅分析），保留评分、依据和审计记录，但不创建模拟成交、不占用账户资金，也不计入策略收益。
- 前端以中性状态展示“仅分析”推荐，并明确区分研究复盘结果与可执行推荐。主库保持 schema 24，分钟库保持 schema 3。

## 2.7.7 - 2026-09-03

- 主库启动时显式执行并校验 `journal_mode=WAL`，不再只依赖连接字符串参数。
- 发布脚本改用 modernc SQLite 可识别的 `_pragma` 参数格式；部署后可直接核验主库实际日志模式。

## 2.7.6 - 2026-09-03

- 恢复发布部署在显式数据库路径下遗漏的 WAL 模式，避免读密集型后台任务长期阻塞证据写入。
- 证据追加、冻结和失败封存事务在读取前先获取 SQLite 写保留锁，消除 `SQLITE_BUSY_SNAPSHOT`。
- 保留前三次失败运行，并允许今天进行一次最终的并发安全恢复尝试。

## 2.7.5 - 2026-09-03

- 修复研究中心2覆盖率分母包含重复代码、退市和ST证券，导致有效快照仍被错误压低至92.26%的问题；覆盖率改为“可信报价数/去重后的策略静态可选主板证券数”，停牌或无价股票仍计入缺失但不再让已退市记录污染分母。
- 证据批次创建和证据项写入增加SQLite busy有限退避，避免启动期并发任务令完整证据因瞬时锁冲突失败。
- 当日失败分析最多允许创建第3次尝试；前三次记录、证据与审计相互独立且均不可覆盖。
- 主库保持 schema 24，分钟库保持 schema 3。

## 2.7.4 - 2026-09-03

- 修复研究中心2把全市场请求期间正常更新的行情误判为截止后数据、导致覆盖率从5,077只降至3,938只并在77.57%处失败的问题；任务启动时间保持不变，证据截止改用快照中最大可信行情时间，五分钟指标仍只使用该分钟之前5个已闭合桶。
- 每个交易日最多允许两次分析尝试：首次失败且仍在上午启动窗口内时可自动创建第2次运行；原失败记录、冻结证据、审计、推荐、账户及收益均不覆盖。
- 主库升级到 schema 24；迁移将既有研究中心2运行保留为第1次，并以“交易日+尝试序号”替代交易日单列唯一约束，不改写其他历史字段。分钟库保持 schema 3。

## 2.7.3 - 2026-09-03

- 研究中心2升级为实际启动时刻前5分钟的证据窗口，并在报告与成交详情展示证据质量、计划/实际时间及成交价格来源和执行模式。
- 全市场、市场宽度、候选分钟线和资金流改用结构化来源链，实行95%市场覆盖与每只候选至少4根已闭合分钟线门槛；完整原始证据继续归档，模型只接收无静默截断的紧凑快照。
- 报告时间和Markdown改由服务端生成，截止参考价与评分加总由程序复核，来源ID按状态、截止时间、股票归属和评分维度校验；每次模型重试分别写入不可变审计记录。
- 超时报告按校验完成后的新行情买入且不再受参考区间限制；午休待买顺延13:00，遗漏卖出固定恢复下一交易日10:00目标分钟，并为SQLite成交与运行写入增加有限退避和原子提交。
- 主库升级到 schema 23；迁移只追加可空的证据质量和成交来源字段，不回填或改写 schema 22 历史报告、推荐、成交、收益、账户或快照。分钟库保持 schema 3。

## 2.7.2 - 2026-09-01

- 修复研究图表已有连续腾讯分钟线时因缺少前收价而误报“部分数据”和整日缺失的问题；分钟覆盖状态与前收价解耦，当前交易日的真实缺失区间按查询终点截断。
- 研究中心2推荐记录隐藏“买入区间”和“净收益”两列，保留收益率与成交信息，缩短表格横向宽度。
- 股票收益率页使用正收益红色、负收益绿色并强化关键数值；最大回撤统一按负值显示，例如 `-3.40%`。
- 本版本不新增数据库迁移，主库保持 schema 22，分钟库保持 schema 3。

## 2.7.1 - 2026-08-31

- 研究中心2推荐记录移除排名与目标买入展示，表头支持拖动并保存顺序，股票入口可查看与研究中心1一致的分时/1分钟K线、成交标记和逐分钟收益。
- 推荐页、详情和收益页在进入或手动刷新时获取最新行情，未平仓收益和账户净值按完整预估卖出成本实时估值；已平仓收益保持实际成交结果。
- 研究中心2模型协议、列表与资金分配统一按最终分和股票代码确定稳定优先级；主库升级到 schema 22，物理删除推荐排名并保存最后有效行情，分钟库保持 schema 3。

## 2.7.0 - 2026-08-29

- Research 1 取消固定时点与手动 AI 分析，改为卖出、启动恢复或资金缺口触发的持久化资金补位事件；连续两分钟内卖出合并为一轮。
- 可部署资金按 `现金 - 待买预留 - max(净资产 × 10%, 5万元)` 计算，不足 5 万元不启动；单轮最多立即买入 2 只，单笔含费不超过 5 万元并按合法整手向下取整。
- AI 最终决策改为严格 JSON 的 `buy_now / wait / reject`；系统校验价格区间、行情时效、停牌/涨跌停、现金缓冲及已有持仓、待买和有效观察股票去重。
- `wait/reject` 不创建正式推荐、不预留资金也不进入收益统计；若仍有资金缺口，完成后 30 分钟重新运行完整分析，14:25 后顺延到下一交易日 09:35 重新分析。
- 新增数据库级事件认领与租约、崩溃恢复、5/10/20 分钟技术失败退避，以及启动时资金核查；原每分钟生命周期扫描、每只股票独立 15 分钟持仓复查和收盘账户快照保留。
- 新增资金补位状态和候选机会接口；设置页、报告页及 OpenAPI 同步升级，旧固定分析设置强制关闭并仅保留列兼容。
- 主库从 schema 20 升级到 schema 21，分钟库保持 schema 3；已有数据库会先在运行目录 `backups` 下生成并校验迁移前 SQLite 快照，迁移只追加事件、机会和运行资金快照结构，不改写既有交易、持仓、收益、重复仓位或历史分析。

## 2.6.0 - 2026-08-29

- “最近 24 小时热词”严格按新闻发布时间筛选；仅在 `data_time` 缺失时回退到入库时间，避免旧新闻重新入库后进入当前窗口。
- 新闻正文统一规范化，并通过精确指纹和字符三元组 SimHash 合并重复转载；热词按去重后的独立新闻篇数统计，同一篇内重复出现只进入解释用出现次数。
- 独立 GSE 分词器整合通用词典、金融词典、股票名、板块名和新闻标签，过滤数字、价格、日期、URL、单字及泛化停用词，并合并明确的同义别名。
- 热词与此前 7 天基线比较，综合平滑突增比、覆盖量和 12 小时半衰期的新近度计分；基线不足时自动退化为覆盖量排行并返回低置信度警告。
- 新增 `GET /api/v1/market/hot/words` 标准数据接口，返回窗口、基线、情绪、来源、置信度和最多 3 条跨来源代表新闻；查询结果缓存 5 分钟并合并并发计算。
- 热点页用可展开排行榜替换矩形树图，保留主要股指与市场情绪，展示提及篇数、窗口占比、基线变化、来源数和置信度；标签页激活期间全天每 5 分钟刷新。
- 补充事件时间、去重、词频、基线降级、排序、代表新闻、情绪、参数边界、Envelope 和前端展示模型测试。
- 本版本不新增迁移，主库保持 schema 20，分钟库保持 schema 3。

## 2.5.1 - 2026-08-28

- 修复东方财富 `push2` 连接异常时 A 股市场宽度显示不可用和虚假零值的问题；按实时接口、延迟接口、腾讯批量行情的顺序独立降级。
- 东方财富延迟行情使用每页 100 条、6 并发、每页最多 2 次重试和 12 秒总超时完整分页，并按市场与代码去重；不足来源声明总量 95% 的样本不会用于“全市场”统计。
- 腾讯降级源只读主库正常上市的沪深股票清单，每批 80 只、6 并发采集；覆盖率达到 95% 后以部分数据返回。
- 三个来源共用上涨、下跌、平盘、涨跌停、日内新高/新低和涨跌幅中位数计算；`asOf` 使用来源报价时间，不再以采集时间代替。
- 增加线程安全的进程内上一成功快照；全部实时来源失败时，24 小时内返回 `stale`，超过时限或进程重启无快照时返回 `unavailable`。
- 市场宽度不可用时所有指标和上涨占比显示为破折号；降级成功显示“数据完整/部分数据 + 降级 N”，仅全部失败时显示“不可用 + 异常 N”。
- 本版本不新增迁移，主库保持 schema 20，分钟库保持 schema 3。

## 2.5.0 - 2026-08-28

- 基金页拆分为异步“基金自选 / 基金排行”，场外基金支持全部、股票型、混合型、债券型、指数型、QDII、FOF，以及日、周、月、3月、6月、1年、3年、今年以来、成立以来和规模排序。
- 场内 ETF 新增宽基、行业主题、跨境、债券、商品、货币分类，以及服务端排名、搜索、独立自选和详情；详情复用统一 K 线并展示实时行情、成交额、换手率、净值、溢折价、份额、规模、净流入、跟踪指数、费率、上市日期和主要持仓。
- 基金净值与阶段收益以东方财富为主、新浪为备；ETF 身份以上交所/深交所为准，实时行情和 K 线按腾讯、新浪、东方财富独立降级。接口统一证券代码、交易日期和净值日期，并返回来源、时间、状态和错误。
- 基金与 ETF 只作信息证据和行业观察，不进入 Research 1/2 候选池，不创建推荐、持仓、账户或模拟交易。
- 主库从 schema 19 升级到 schema 20，分钟库保持 schema 3；迁移只追加排名缓存、详情快照和 ETF 自选结构，不改写历史研究、推荐、账户、成交、持仓、费用或收益。

## 2.4.0 - 2026-08-28

- 新增受控知识库，支持 TXT、Markdown、可提取文本 PDF 和既有 Research 1/2 报告草稿；文档内容按不可覆盖版本及 SHA256 保存。
- 文档版本采用 `draft / approved / rejected / superseded` 生命周期；只有用户批准且未被替代的版本可进入 FTS5 检索，AI 与系统流程不能直接批准。
- 新增长期记忆候选队列；AI 可生成候选，但必须由用户批准后才生成受控知识版本，拒绝和批准原因均被保存。
- Research 1 增加知识库与记忆管理入口，Research 2 只消费已批准知识；两套研究仅在默认关闭的实验模式下检索。
- 知识内容始终作为不可信外部线索，提示词明确忽略其中的指令；所有事实仍须用本次截止时间前的市场证据重新验证，并保存检索命中、采用/拒绝原因与验证状态。
- 主库从 schema 18 升级到 schema 19，分钟库保持 schema 3；迁移只追加知识、版本、审批、FTS5、候选和检索审计结构，不改写历史研究、推荐、账户、成交、持仓、费用或收益。

## 2.3.0 - 2026-08-28

- 两套研究新增按作用域、阶段和版本唯一的 Prompt 模板归档；模板使用 gzip BLOB 保存并记录 SHA256，数据库拒绝修改或删除已发布版本。
- 每次模型调用按来源运行、调用序号和尝试次数唯一保存最终 Prompt、证据、工具清单、模型参数、原始响应、可选修复响应、修复日志及脱敏清单；二进制载荷均使用完整 gzip bundle 与 SHA256，已存记录不可覆盖。
- 新增审计运行状态，运行身份和创建时间不可修改，仅允许推进状态、载荷数量、错误和更新时间；旧历史运行不强制回填，缺失审计可明确标记为 `legacy_unavailable`。
- 新增受控研究回放及不可变结果；回放创建后固定来源研究、模型配置与截止时间，只允许更新执行状态、开始/完成时间和错误信息。
- 主库从 schema 17 升级到 schema 18，分钟库保持 schema 3；迁移只追加 5 张审计/回放表、索引和保护触发器，不改写既有研究、证据、账户、推荐、成交、持仓、费用、收益、绘图或题材历史。

## 2.2.0 - 2026-08-28

- 市场热点增加“每日炒作题材”，题材生命周期固定为“观察 → 发酵 → 加速 → 分歧 → 退潮”，并按交易日展示不可覆盖的排名、热度、摘要、成分股和催化剂快照。
- 沪深交易日 15:10 自动汇总热门话题、热点事件、新闻电报和概念资金流；来源独立超时、失败或空数据不会拖垮有效来源，同日重复运行保持幂等。
- 题材名称与别名统一归一化；题材、别名和各类公开 ID 唯一，同一题材的同一事件指纹去重，同一事件的同一来源链接去重。
- 催化剂可信度与题材生命周期相互独立；不同来源的支持、反驳和中立主张并列保留，来源可用时间用于研究截止过滤，并可关联永久证据项。
- 新增题材列表、详情、历史快照和催化时间线 API；公开读取统一按冻结时点截断，界面支持日期、阶段、搜索和排序，并并列展示相互冲突的来源。
- Research 1、Research 2 仅在默认关闭的实验证据模式下读取题材快照和催化主张；两套研究都严格使用各自截止时间，ETF 与基金成分只作背景，不进入候选或交易。
- 每日题材快照与来源主张由数据库拒绝修改或删除；快照催化关联和成分股达到父快照声明数量后禁止追加，证据关联使用 NULL 安全的表达式唯一键，避免绕过去重或改写冻结内容。
- 主库从 schema 16 升级到 schema 17，分钟库保持 schema 3；迁移只追加 8 张题材/催化表、索引与不可变触发器，不改写既有研究、账户、推荐、成交、持仓、费用、收益或绘图历史。

## 2.1.0 - 2026-08-28

- 新增股票、指数和场内 ETF 的统一图表接口与渲染核心，支持 `1/5/15/30/60` 分钟、日、周、月、季、年，以及 `none/qfq/hfq` 复权；腾讯、新浪、东方财富按独立适配器降级，旧图表调用方保持兼容。
- 新增 MA、BOLL、VOL、MACD、KDJ、RSI，以及测距、趋势线、射线、水平线、波段、斐波那契回撤；图表可展开集合竞价、分时成交和逐笔成交，并展示来源、更新时间、缺失区间与错误。
- 股票详情、主要指数和 ResearchTradeChart 复用同一渲染核心；研究复盘仍使用真实成交记录、全费用分钟净盈亏、交易日分隔、缺口和来源异常，港美行情明确保留 legacy 数据适配边界。
- 新增按作用域、资产类型、市场、代码、周期与复权方式唯一隔离的当前 K 线绘图文档；当前文档使用非负修订号并支持可审计软删除。
- 新增不可覆盖的绘图修订历史；同一文档的修订号唯一，数据库拒绝修改或删除既有修订。
- 主库从 schema 15 升级到 schema 16，分钟库保持 schema 3；迁移只追加绘图表、索引与新表保护触发器，不改写既有研究、账户、推荐、成交、持仓、费用或收益历史。

## 2.0.0 - 2026-08-28

- 新增冻结证据集与证据项基础模型；证据以 `available_at` 判定是否落在研究截止时间内，`collected_at` 仅作采集审计，并保留来源引用、摘要、原始载荷与内容哈希。
- 两套研究运行新增可空的策略版本、证据配置版本和证据集关联；实验性证据开关默认关闭。升级不回填这些关联，不改写既有账户、推荐、成交、持仓或收益历史。
- 分钟库新增多资产多周期行情、集合竞价快照和逐笔成交缓存，为后续行情扩展提供隔离的数据基础。
- 发布流程在主库或分钟库任一 schema 上升时进入维护迁移；每个库单次最多升级一个版本，任一库降级均须走带归档回执的回滚，并对主库和分钟库成对备份、校验和恢复。
- 主库升级到 schema 15，分钟库升级到 schema 3；迁移为可重复验证的增量变更。

## 1.8.7 - 2026-08-28

- 修复东方财富全市场列表返回数字键对象时研究中心2证据采集失败的问题；兼容数组和对象两种响应，增加端点轮换、重试及完整分页回退。
- 研究中心2以结构化最终分作为入库判定：超过50分即入库，不再被报告中的“空仓”“不推荐”等文字否决；分项加总不一致只告警，不阻断入库。
- 本版本不新增数据库迁移，主库保持 schema 14，分钟库保持 schema 2。

## 1.8.6 - 2026-08-27

- 研究中心2新增独立报告邮件设置；升级时复制完整的旧版 SMTP 配置并自动开启，研究中心1不显示或控制这些字段。
- 成功、无推荐及错过交易窗口的报告会在交易处理后异步发送纯文本邮件；发送状态持久化，失败后按 1、3、10 分钟最多重试三次，应用重启可恢复。
- 报告列表与详情增加邮件投递状态，新增 SMTP 测试接口；主库升级到 schema 14，分钟库保持 schema 2。

## 1.8.5 - 2026-08-27

- 两个研究中心的设置页改为按中心隔离策略控制项：研究中心1只显示原自动分析时间与持仓复查设置，研究中心2只显示隔夜强势自动策略开关。
- 通用设置、数据接口和模型回退配置继续共享；本版本不新增数据库迁移，主库保持 schema 13，分钟库保持 schema 2。

## 1.8.4 - 2026-08-27

- 将原研究中心更名为“研究中心1”，把共享设置并入两个研究中心的页签，移除独立设置菜单并兼容旧 `/settings` 地址。
- 新增完全独立的“研究中心2”隔夜强势策略：交易日 09:50 开始采集、09:55 冻结证据，报告尽量在 10:00 前完成；正常 10:00 买入、下一交易日 10:00 卖出，迟到报告按首个可成交分钟处理并明确标记。
- 研究中心2使用独立 12,000 元复利模拟账户；1—3 只实际可买股票等额分配现金，按 100 股整手向下取整并计入佣金、印花税、过户费和滑点。
- 新增腾讯优先、东方财富回退的全市场与分钟行情链路、严格交易日校验、结构化评分校验、自动调度开关、推荐/报告/收益页面及隔夜辅助指标。
- 主库升级到 schema 13，新增研究中心2运行、推荐、交易、账户和净值快照表；分钟库保持 schema 2。

## 1.8.3 - 2026-08-27

- 完成对 1.8.2 分析调度、持仓复查、私人分钟线密钥和发布文档变更的整体复核，以新的不可变版本身份重新发布。
- 本版本不新增业务逻辑或数据库迁移；主库保持 schema 12，分钟库保持 schema 2。

## 1.8.2 - 2026-08-27

- 程序启动后会检查最近一个错过的自动分析节点并立即补跑；定时分析保存精确计划节点，跨重启可去重，失败后在当日开盘时段每 5 分钟持续重试，后续新节点自动取代旧节点。
- 过期的持仓复查不再静默跳过；首轮仍从配置时间开始，之后每只股票按自身实际完成时间独立计算间隔。模型、网络、解析及关键数据不足均每 5 分钟重试至当日收盘。
- 午休期间暂停自动分析和复查重试，跨午休顺延到 13:00；临近收盘无法继续重试时恢复为下一交易日的正常计划。
- 私人分钟线密钥移除源码默认值，支持独立密钥文件和 `secret://` 安全引用；补充接入、版本发布与大模型提示词文档，并清理不再使用的根目录截图。
- 重新校验发布内嵌股票主数据；主库保持 schema 12，分钟库保持 schema 2，不执行数据库迁移。

## 1.8.1 - 2026-08-27

- AI 网络失败增加指数退避；共用端点的回退配置切换前增加冷却。
- 研究前主动刷新市场快讯，并恢复受控的后台定时刷新。
- 财联社接口异常时回退网页来源，新浪快讯可独立维持最新窗口。
- 主库保持 schema 12，分钟库保持 schema 2。

## 1.7.10 - 2026-08-25

- 股票推荐记录将“买入金额、卖出金额、当前金额”改为“买入价、卖出价、当前价”，统一展示每股价格而非整个仓位金额。
- 买卖价按实际模拟成交数量计算加权成交均价；当前价只展示未平仓仓位的最新行情，已平仓仓位显示为空。
- 保留原净金额 API 字段以兼容既有调用，并新增三个明确的每股价格字段；主库继续使用 schema 12，分钟库继续使用 schema 2。

## 1.7.9 - 2026-08-25

- 底部导航栏的 GitHub 图标右侧显示运行制品版本号，版本直接来自后端嵌入的发布清单，不再由界面手工维护。
- “关于软件”中的当前版本与研究流程说明改为动态展示，避免版本升级后遗留过期文案。
- App、API、前端版本、发布文档和 Git tag 同步升级到 1.7.9；发布脚本拒绝构建或部署未指向当前提交的版本 tag。主库继续使用 schema 12，分钟库继续使用 schema 2。

## 1.7.8 - 2026-08-25

- 修正腾讯 A 股实时行情最高价、最低价字段错位，并对异常 OHLC 关系直接报错，避免错误报价进入研究提示词。
- 来源采集与各分析阶段使用真实完成时点；业务失败和过期数据不再计作成功来源，也不再作为有效正文送入模型。
- 最近 5 个交易日的推荐作为软性分散上下文；允许重复持仓，但重复推荐需说明新增证据，同等质量时优先新标的。
- 修复线上发布指针与数据库 schema 版本错位；主库继续使用 schema 12，分钟库继续使用 schema 2，不执行数据库迁移。
- schema 12 运行时校验兼容历史纠正仓位的正常平仓状态，避免已完成卖出后阻断应用启动。

## 1.7.7 - 2026-08-24

- 模拟账户改为自始固定投入 `500,000 元`，停止计划入金，并将历史资金流水与净值快照统一重算到固定本金口径。
- 取消 10 个持仓、每轮最多 2 只以及同股重复持仓限制；每笔推荐继续按约 `50,000 元`目标独立买入和管理，现金预留仍防止并发超支。
- 收益率页移除“资金与持仓容量”板块，保留账户摘要、资金流水、净值曲线与独立仓位明细。
- 受控恢复 2026-08-24 12:03 分析报告中的中国平安推荐，按午休后 13:00 开盘证据补记独立模拟买入。
- 主库升级到 schema 12，分钟库继续使用 schema 2。

## 1.7.6 - 2026-08-21

- 单只 `50,000 元`从硬上限改为实际现金支出目标：按包含滑点、佣金和过户费的成本，选择首次严格超过目标的最小整手；目标仓位现金不足时回退到可承担的最大整手。
- 保留普通沪深 A 股 100 股、科创板 200 股的最小申报单位；仅在连一手也无法承担时跳过，不再因高价股票一手超过目标而拒绝推荐。
- 基于 1.7.5 已完成的 XD/XR/DR 除权除息名称修复，受控补记中际旭创在 2026-08-21 14:35 的历史模拟买入；原“错过—资金不足”事件继续保留供审计。
- 主库继续使用 schema 11，分钟库继续使用 schema 2，不执行数据库迁移。

## 1.7.5 - 2026-08-21

- 修复除权除息日行情名称带 `XD`、`XR` 或 `DR` 前缀、或被行情源缩写时，持仓股票被误判为其他股票而无法卖出的问题；股票代码仍必须严格一致，无关名称仍会被拒绝。
- 新增受控、幂等的历史卖出纠正，仅按原 13:30 AI 卖出决策所引用的已落库行情补记模拟卖出，不使用后续或当前行情；13:45、14:00 和 14:15 的失败重试记录继续保留供审计。
- 主库继续使用 schema 11，分钟库继续使用 schema 2，不执行数据库迁移。

## 1.7.4 - 2026-08-21

- 推荐详情的分时与1分钟K按交易日增加竖向虚线，价格区和成交量区同步分隔。
- 删除最后分钟柱重复的“最新”圆点；新增全局价格横线开关，统一控制最新价、买入均价和卖出均价。
- 价格横线默认隐藏，选择保存在当前浏览器并对全部推荐详情生效。
- 主库继续使用 schema 11，分钟库继续使用 schema 2，不执行数据库迁移。

## 1.7.3 - 2026-08-20

- 领域 HTTP handler 直接依赖窄服务接口，删除透明 service 转发层和单一 God adapter，降低跨层修改成本。
- 引入统一运行时协调器，后台任务支持取消和有界等待；研究读请求继续传递 HTTP 上下文。
- 前端统一轮询与拖拽生命周期，研究 Client 开始使用生成契约类型；network-audit 移出主程序模块。
- Windows 与 Linux 启动、重启、发布脚本统一为 Web-only，不再传递或识别废弃的 `--web` 参数。
- 主库继续使用 schema 11，分钟库继续使用 schema 2，不执行数据库迁移。

## 1.7.2 - 2026-08-20

- 设置页收敛为通用、数据接口、AI 分析和配置管理四个领域，分钟来源与 AI 模型均可排序和独立启停。
- 自动分析开关只控制定时触发，手动研究与持仓生命周期继续使用同一研究后端。
- 移除普通诊股、Prompt 模板及其专用 HTTP/导出入口，市场行情、股票自选、基金与研究中心保持不变。
- 主库升级到 schema 11，增加分钟来源排序和持仓复查时间配置；分钟库保持 schema 2。

## 1.7.1 - 2026-08-19

- 新增幂等的历史资金与买入纠正命令，按明确行情来源补记指定的两笔错过成交。
- 前两期计划入金可按历史有效时间登记，单位净值和 TWR 不受外部资金流影响。
- 历史纠正保留原事件并追加审计时间线，后续卖出判断从指定交易日 09:50 重新锚定。

## 1.7.0 - 2026-08-19

- 模拟账户从升级后的下一个交易日起分四次追加资金，累计净入金上限为 50 万元；所有入金使用独立审计流水且不计入策略利润。
- 账户收益改为剔除外部资金影响的时间加权收益率（TWR），并增加净值快照、最大回撤、胜率、盈亏比、费用、换手率和资金利用率等评估指标。
- AI 推荐容量从“任一持仓即阻断”改为最多 10 个持仓/待买敞口，每个待买任务预留 5 万元且每轮仍最多推荐 2 只。
- 持仓复查保持每只股票上下文隔离，卖出判断最多 5 路并发；共享市场数据按 15 分钟桶去重，慢模型不再阻塞全部持仓。
- 主库升级到 schema 10，分钟库保持 schema 2；现有现金、持仓、交易和历史收益记录原样保留。

## 1.6.9 - 2026-08-19

- 分钟图数据源模式改为优先级：私人源、腾讯、新浪与 AKShare 可组成自动回退链，不再互斥。
- 当前来源报错、返回空数据、数据不合规或覆盖不完整时自动尝试下一个已启用来源，完整覆盖后停止。
- 研究中心金额、价格、数量、费用、成交量和净收益统一采用三位逗号分隔，并限制合理小数位。
- 设置页显示实际回退顺序和每个来源开关；主库继续使用 schema 9，分钟库继续使用 schema 2。

## 1.6.8 - 2026-08-19

- 股票推荐与收益详情新增完整持仓期分时/一分钟 K 线、成交量、十字光标、买卖点和全成本净收益。
- 分钟图表优先读取本地缓存，只在打开详情和人工刷新时补齐数据；缺失交易日如实展示。
- 全站 Markdown 预览统一为类 ChatGPT 的左对齐阅读排版，长表格独立横向滚动。
- 主库继续使用 schema 9，分钟库继续使用 schema 2；不执行数据库迁移。

## 1.6.7 - 2026-08-18

- 正式收敛为本机 Web-only 架构，删除 Wails 桌面运行时、生成绑定、打包配置和双前端桥。
- 以显式 `/api/v1` typed HTTP API 替换 72 个反射 RPC，按系统、设置、分组、股票、基金、市场、AI、研究和导出领域拆分。
- 前端统一使用领域 API Client 和浏览器事件/WebSocket 运行时；市场行情 12 个页签及全部现役功能保持不变。
- 主库继续使用 schema 9，分钟库继续使用 schema 2；不执行数据库迁移。

## 1.6.6 - 2026-08-18

- 将 1.5 旧策略、旧主库分钟缓存、收益状态和回测表制作永久完整归档后移出活动主库。
- 新增离线主库压缩、归档校验及数据库联动回滚，schema 9 失败时恢复 1.6.5 与 schema 8。
- 保留市场行情、股票自选、普通诊股、基金、研究中心和完整情感历史；分钟库保持 schema 2。

## 1.6.5 - 2026-08-18

- 删除新推荐的等待激活流程，AI 推荐通过交易校验后按最新行情直接模拟买入；午休或盘后完成时顺延到下一有效交易时段。
- 买入只尝试一次，不可交易和资金不足分别形成明确终态；账户、持仓、交易和买入事件使用同一事务落库。
- 卖出判断从下一交易日 09:50 开始，并固定在上午 15 分钟序列及午后 13:00 起的 15 分钟序列，不因模型耗时漂移。
- 研究中心删除新流程的激活条件和激活窗口展示，历史激活字段与时间线只在旧记录详情中保留。
- 主库 schema 升至 8，迁移旧待激活记录进入一次性待买队列；分钟库保持 schema 2。

## 1.6.4 - 2026-08-18

- 生命周期每 15 分钟采集实时行情、分钟量价、指数/行业和增量事件，并按激活条件定向补充资金、公告和互动来源。
- 实时行情或分钟量价失败时不调用 AI；未激活阶段每轮抵扣 15 分钟、累计最多抵扣 30 分钟。
- 激活和卖出必须引用本轮实时行情与分钟量价来源，推荐详情可折叠查看每轮证据快照与来源状态。
- 主库 schema 升至 7，分钟库保持 schema 2；现有推荐、独立会话、交易和收益历史保持不变。

## 1.6.3 - 2026-08-17

- 自动分析仅在沪深交易日开盘时段启动；休市时不调用模型，也不创建跳过或失败报告。
- 人工分析不受日期和时间限制，盘后完成的推荐从下一交易日继续判断。
- 未激活推荐统一按累计 4 小时真实开盘交易时长计时，午休、盘后、周末和休市日不消耗时长。
- 恢复 2026-08-17 盘后生成的中际旭创和中微公司推荐，保留原失效事件并追加纠正记录。
- 研究中心默认打开并优先展示“股票推荐记录”；主库 schema 升至 6，分钟库保持 schema 2。

## 1.6.2 - 2026-08-17

- 研究分析与生命周期判断改为流式模型调用，活跃推理持续等待，仅连续 300 秒无有效事件才超时。
- 瞬态故障在当前模型最多尝试 5 次；鉴权、参数和协议不兼容等不可恢复错误立即顺延下一模型。
- 研究报告详情新增结构化模型调用记录，展示阶段、次数、状态、最后活动、错误分类和后续动作。
- 取消后台分析与生命周期的固定整轮超时；CLI 默认无限等待并保留可选人工超时。
- 主库 schema 升至 5，分钟库继续使用 schema 2，历史记录保持不变。

## 1.6.1 - 2026-08-15

- AI 模型配置表改为唯一回退顺序，并为每个模型增加独立启用开关。
- 研究分析和生命周期判断统一为单次 30 秒、每模型最多 5 次，连续失败后再切换下一模型。
- 研究中心新增手动“开始 AI 分析”按钮；运行期间禁用，报告生成后恢复。
- 修复 Web RPC 对 `(结果, nil)` 的解包、分析列表负载和 Windows 启动脚本识别。
- 主库 schema 升至 4，保留全部历史表与研究记录。

## 1.6.0 - 2026-08-14

### 全新 AI 研究流程

- 删除 1.5.x 策略、cohort、冻结回放、runtime guard、preflight、策略审计、兼容链路和独立 AI 智能体。
- 新增大盘、板块、个股、决策四级 AI 分析，整合现有资讯、行情、研报、公告、财务、概念和资金数据。
- 每轮最多入库两只可交易沪深 A 股并允许空仓；最终报告强制包含固定推荐表格。
- 每只股票使用完全隔离的生命周期会话，每 15 分钟由 AI 判断激活、失效、持有或卖出。

### 模拟交易与收益

- 新增 10 万元全局模拟账户、5 万元单股上限、整手规则、T+1、涨跌停和停牌成交约束。
- 统一计入佣金、最低佣金、卖出印花税、过户费及双边滑点。
- 仅保留净收益额和净收益率；删除基准收益率、超额收益率、XIRR 和旧收益重算。

### 数据与界面

- 主库升级到 schema 3，新增独立 `research_v160_*` 表；旧表和历史行保留但运行代码彻底断开。
- 删除旧策略 guard/immutable 触发器，分钟库继续使用 schema 2。
- 研究中心仅保留 AI 分析报告、股票推荐记录、股票收益率三个页签。
- 设置页新增 AI 分析开关、配置和自动分析时间；市场行情保持为独立读取模块。
- 发布身份收敛为 App `1.6.0` 与数据库版本，不再暴露策略版本和配置 hash。
