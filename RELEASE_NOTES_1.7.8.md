# Go-Stock 1.7.8 发布说明

1.7.8 修复研究阶段的数据可信度与时点问题。腾讯 A 股实时行情现在按正确字段读取最高价和最低价，并在 OHLC 关系异常时拒绝该来源，避免错误报价进入模型。

来源采集完成时间与每个分析阶段的证据截点分别记录。接口返回 `success:false`、失败状态或过期状态时会计入失败来源，其正文不再作为有效研究材料。正常但为空的来源仍按空结果处理。

最近 5 个交易日、最多 20 条推荐会作为软性分散上下文提供给板块、个股和最终决策。系统仍允许同股重复持仓；同等质量时优先新标的，重复推荐需要说明相对上次的新增证据。

本版本不修改模拟账户、单笔约 5 万元的下单算法或数据库结构。主库保持 schema 12，分钟库保持 schema 2。
