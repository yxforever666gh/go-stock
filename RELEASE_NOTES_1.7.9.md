# Go-Stock 1.7.9 发布说明

1.7.9 在底部导航栏的 GitHub 图标右侧显示当前运行制品版本，例如 `GitHub · v1.7.9`。版本由后端嵌入的发布清单提供，因此 App、API、前端显示和发布制品使用同一版本来源。

“关于软件”中的当前版本和研究流程说明同步改为动态展示，避免升级后继续出现写死的旧版本号。发布脚本会校验同名 Git tag 必须存在并指向当前提交，防止版本或 tag 漏同步。

本版本不修改研究、交易或数据库结构。主库保持 schema 12，分钟库保持 schema 2。
