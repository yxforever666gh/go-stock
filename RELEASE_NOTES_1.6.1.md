# Go-Stock 1.6.1 发布说明

1.6.1 完善了 1.6.0 的 AI 分析运行与本地 Web 体验，数据库主 schema 为 4，分钟库 schema 仍为 2。

## AI 调用与回退

- 设置页模型表从上到下就是实际回退顺序，每个模型可单独启用或关闭。
- 每个模型最多尝试 5 次，单次硬超时 30 秒；连续失败后才切换下一启用模型。
- 研究分析和推荐生命周期判断共用同一规则，不再叠加 HTTP 客户端隐式重试。

## 研究中心

- AI 分析报告页新增“开始 AI 分析”按钮。
- 分析运行期间按钮禁用并持续读取报告状态，报告成功、空仓、跳过或失败后恢复。
- 修复报告列表、详情和模拟账户在本地 Web RPC 中的返回值解包与负载问题。

## 启动与兼容

- 修复 Windows 启动脚本对监听进程路径不可读时的发布身份校验。
- 保留 1.6.0 的全部研究记录与历史表，不清库、不重置模拟账户。
