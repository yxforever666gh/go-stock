# Go-Stock 1.6.2 发布说明

1.6.2 修复长推理被固定超时误杀的问题，数据库主 schema 为 5，分钟库 schema 仍为 2。

## 流式推理与回退

- 研究中心的 OpenAI Responses、Chat Completions 和 Anthropic Messages 调用统一使用流式接收。
- 连续 300 秒没有有效推理、心跳、状态或正文事件才判定超时；流仍活跃时不限制总推理时长。
- 网络、限流、服务端错误和断流在当前模型最多尝试 5 次；鉴权、参数、模型不存在和协议不兼容立即顺延下一模型。
- 后台分析和生命周期处理不再受固定整轮截止时间影响。

## 调用诊断

- AI 分析报告详情新增结构化模型调用记录，运行中可查看等待、推理、生成、重试、顺延和最终结果。
- 记录包含阶段、模型、API 格式、次数、耗时、最后事件、错误分类和后续动作。
- 错误信息会脱敏并限制长度，不保存 API Key、完整 prompt 或原始响应正文。

## 数据与兼容

- schema 5 为现有分析运行表增加模型调用诊断字段，旧记录自动补为空数组。
- 不删除既有分析、推荐、交易、持仓或历史策略表。
