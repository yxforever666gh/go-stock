# Go-Stock 1.7.3 发布说明

1.7.3 是一次不改变产品功能和数据库结构的工程收敛版本。市场行情、股票自选、基金、情感分析、研究中心、定时研究、持仓生命周期和外部通知保持原有行为；主库继续使用 schema 11，分钟库继续使用 schema 2。

后端删除了只做转发的 service 包装和单一大适配器，HTTP handler 改为依赖股票、基金、分组、市场、配置和 AI 等窄领域接口。后台任务由统一协调器负责取消与有界等待，研究查询继续传递请求上下文，退出和测试行为更可控。

前端热点轮询与股票分组拖拽使用可复用生命周期组件，修复离开页面后定时器和事件监听残留；研究 API Client 开始消费 OpenAPI 生成类型。开发用网络审计工具移到独立模块，不再增加主程序的编译与维护面。

Windows 的 `启动项目.cmd`、重启和发布脚本，以及 Linux 管理脚本均统一为 Web-only 启动，不再使用废弃的 `--web` 参数。可运行 `启动项目.cmd -NoBrowser` 只启动服务而不打开浏览器。
