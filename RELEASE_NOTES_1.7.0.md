# Go-Stock 1.7.0 发布说明

1.7.0 将模拟账户扩展为最多十只并行敞口，并从升级后的下一个实际运行交易日起分四次追加资金，累计净入金达到 50 万元。追加资金拥有独立审计流水，不会被计入策略利润。

账户总收益采用时间加权收益率（TWR），研究中心同时展示净收益额、累计投入回报率、最大回撤、胜率、盈亏比、费用、换手率、资金利用率、平均持有时间和样本充足度。

行业集中度已预留结构化指标位置；当前历史推荐没有可靠的结构化行业归属，因此界面明确显示“暂无结构化行业数据”，不会从 AI 自由文本中猜测行业并产生误导。

每轮 AI 仍最多推荐两只股票，单只现金预算仍不超过 5 万元。持仓生命周期保持每只股票独立上下文，卖出判断最多五路并发，并继续执行 T+1、交易费用、滑点、停牌和涨跌停限制。

本版本主库 schema 升级为 10，分钟库保持 schema 2。迁移不会修改现有账户现金、持仓、交易或历史研究记录。
