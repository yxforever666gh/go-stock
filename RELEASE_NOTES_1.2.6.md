# 1.2.6

`1.2.6` 聚焦两件事：一是把股票收益率页里的“全库按交易日收益走势”补成真正可核对的 strict 视图，二是把市场资讯 AI 报告的新数据补齐 provider 展示，避免 `Provider / 模型` 列继续只剩模型名。

### 这次为什么要修

- 用户在收益率页里需要一个不占大窗口的全库图表视图，能直接看每天赚了多少钱、收益率怎么走，而不是只看单条推荐记录。
- 原先“单日变化”会被历史累计成本池严重摊薄，视觉上和用户对“今天实际在场的钱赚了多少”的理解不一致。
- 页面顶部“总净收益率”和弹窗最后一个累计点在一段时间内没有走同一套 strict 聚合链路，同一时点会出现两套结果。
- `AI分析报告` 页前端已经支持 `provider / model` 组合展示，但最近的市场资讯报告写库时没有稳定写入 `provider_name`，导致列表里常常只显示 `gpt-5.4`。

### 本版行为变化

- 股票收益率页新增“查看全库收益走势”弹窗，小窗内展示：
  - `累计走势`
  - `单日变化`
- 单日收益率改为按“当日参与盈亏计算的持仓成本”计算：
  - 不再被历史几十万成本池摊薄
  - 卖出前后、首日持仓与当日新开仓的口径都已统一
- 全库收益弹窗最后一个累计点与页面顶部总净收益率已对齐，均按同一套 strict 明细计算。
- Web 模式补齐 `/build/appicon.png` 与 `/favicon.ico` 静态路由，浏览器端资源验证更干净。
- 新生成的市场资讯 AI 报告现在会稳定写入 `provider_name`，`AI分析报告` 页的 `Provider / 模型` 列会显示完整组合。
- 历史空 `provider_name` 记录本次不做回填，继续按模型名展示。
- `diemeng` 私有分钟线链路现在会按代理环境自动切换 host：
  - 命中 `mohomoparty` 系统代理时保留原配置
  - 无代理直连时自动改到 `data.diemeng.chat`
- `diemeng` 的分钟线请求、自检和网络审计展示已统一到同一套有效地址，国内直连下不再撞到 `diemeng.chat` 的证书域名不匹配问题。
- `cls_telegraph_api`、`cls_telegraph_web`、`sina_live_news` 三条资讯源增加“直连 / 设置代理”竞速抓取：
  - 无代理时优先直连
  - 有设置代理时直连与代理并发，自动使用最快成功结果
- 财联社 / 新浪资讯链路的解析改成健壮模式；即使源站字段变动、空列表或局部缺字段，也不会再把整个抓取链路打成 panic。
- `network-audit` 现在会给这三条资讯源附带 `networkPath` 与 `fallbackUsed`，可以直接看到结果是直连成功还是代理成功。
- 股票收益率页中的“当前价格”现在统一代表最新行情快照或最近收盘价；strict 口径下不再把推荐生成时的历史 `stock_current_price` 快照误当成“当前价格”展示。
- 市场资讯历史激活规则修复流程现在会优先恢复被旧版本写坏的双路径规则：如果旧记录因为错误的 breakout 阈值或错误的 `pullback` 路径被打成“偏离过大”，系统会尝试从原始信号文本重建正确的 `pullback / breakout` 结构化规则，而不是直接继续保留坏 JSON。
- 新增 CLI 命令 `repair-market-summary`，用于重复执行这类历史规则修复；本轮已实测修复当前库 23 条市场资讯候选中的 11 条规则升级/重建案例。
- 严格回算结果现在会同步回写推荐原表的 `activation_status`，避免收益率页已经显示 `activated`，但 `股票推荐记录`、开盘复核等仍读取原表的地方继续看到旧的 `pending`。
- strict `dirty` 标记的展示口径已收紧：只有真正还需要下载/回算的候选才会显示“待回算”；已经得出终态的 `invalid / skipped / ineligible` 或 `analysis_only` 记录不会再被 dirty 覆盖回 pending，终态落库后对应 dirty 也会自动清掉。

### 对已有数据的影响

- 旧的收益率累计结果不会整体改口径；本次只修正“单日变化”的分母和“顶部总收益率 vs 弹窗累计点”的一致性。
- 历史 `AI分析报告` 中 `provider_name` 为空的记录不会被数据库脚本批量回填。
- 从 `1.2.6` 开始，新生成的市场资讯报告会优先按当前 AI 配置解析 provider，找不到时再按模型名做保底推断。

### 本次同时更新的发布物

- `CHANGELOG.md`
- `README.md`
- `RELEASE_NOTES_1.2.6.md`
- `wails.json` 版本号
- About 页默认版本说明文案

### 升级建议

- 升级后请重新构建前端并重启当前运行实例，避免继续使用旧的静态资源和旧二进制。
- 如果你主要用 Web 模式，请优先验证：
  - 股票收益率页顶部总净收益率
  - “查看全库收益走势”弹窗最后一个累计点
  - `AI分析报告` 页中新生成记录的 `Provider / 模型` 列
- 如果你依赖私有分钟线，请优先验证：
  - 无代理直连下 `diemeng` 分钟线、自检与 `network-audit`
  - 有 `mohomoparty` 代理时是否仍保持原 host 行为
- 如果你依赖市场资讯快讯，请优先验证：
  - `cls_telegraph_api`
  - `cls_telegraph_web`
  - `sina_live_news`
  - `network-audit` 中这三项是否能看到 `networkPath / fallbackUsed`
