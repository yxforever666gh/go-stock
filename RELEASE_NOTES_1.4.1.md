# go-stock 1.4.1 Release Notes

## 版本定位

1.4.1 聚焦“补位闭环 + 诊断面板 + 候选池质量增强”。本次升级不扩大推荐数量、不放松硬规则，也不改变 `conditional / analysis_only / activation_status` 的业务语义；重点是让 1.4.0 的候选池前置和 AI 顺延队列形成可复盘、可补位的闭环。

## 主要变化

- 市场总结推荐默认写入 `summary_version = 1.4.1`，`current` 策略阶段切到 `V1.4.1`。
- 第一轮 AI 推荐保存后，如果生产候选少于 2 只，系统会从剩余 verified candidates 中最多触发一次二轮补位。
- 二轮补位只输出“推荐股票池”表格和补位说明，不重写完整市场总结正文。
- 二轮补位继续走同一套解析、保存前交易计划硬校验、同日去重、生产候选上限和激活门控，硬规则不过仍会降级或跳过。
- 市场总结正文末尾追加“补位说明”，记录是否触发补位、补位候选和仍被拦截的主要原因。
- 新增 `MarketSummaryRunDiagnostic` 诊断表，记录候选池数量、AI 输出数量、保存数量、生产/仅分析数量、拦截原因 Top 5、空转状态和运行备注。
- 推荐收益统计页新增“市场总结诊断”面板，可随策略阶段筛选查看最新诊断、拦截原因和最近连续空转次数。
- 候选池评分新增板块强度、近期失败记录和数据完整性因子，并输出 `scoreBreakdown`，评分只影响排序和送 AI 优先级。

## 风控说明

1.4.1 的补位目标是“让后面的候选有机会被硬规则验收”，不是强行凑满入库数量。生产候选目标 2 只只用于决定是否触发二轮 AI；如果剩余候选仍不满足保存前硬规则或激活门控，系统不会放宽阈值。

## 升级后建议观察

- `市场总结诊断` 面板里的候选池数量是否稳定，避免 AI 在空候选上空转。
- `AI输出 / 保存` 与 `生产 / 仅分析` 的比例，判断问题集中在 AI 交易计划质量还是硬规则拦截。
- 拦截原因 Top 5 是否长期集中在价格锚点偏离、盈亏比/止损空间不达标或激活规则缺失。
- 普通强势行情下，连续空转次数是否明显下降。
