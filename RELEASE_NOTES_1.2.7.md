# 1.2.7

`1.2.7` 的核心是把“收益率明细”和“收益率统计”彻底分层。前者继续负责看单条推荐、分钟回放和补算链路，后者专门负责回答一个更关键的问题: 这套策略到底有没有稳定跑赢基准。

### 这次为什么要做

- 只看“盈利了没有”并不能说明策略好坏，因为大盘整体上涨时，很多方案都会一起赚钱。
- 只拿某个时点一次性买入的指数去比较，也不适合持续分批买入、每天都可能开新仓的策略。
- 原来的收益率页虽然已经有 strict 汇总和全库走势弹窗，但统计指标、图表和个股大表格混在一起，不适合先做策略层判断。

### 本版新增什么

- `研究中心` 新增独立栏目 `收益率统计`
- 统计页直接展示：
  - `策略收益率`
  - `沪深300（现金流匹配）`
  - `超额收益率`
  - `策略 XIRR`
  - `基准 XIRR`
  - `超额 XIRR`
  - `最大回撤`
  - `跑赢基准占比`
  - `超额收益中位数`
- 页面内直接内嵌“全库收益走势”图表：
  - `累计走势`
  - `单日变化`
- 图表和统计页继续沿用 strict 口径与现金流匹配 benchmark，不额外引入第二套统计逻辑。

### 现在怎么用

- 如果你先想判断策略值不值得继续优化，先看 `收益率统计`：
  - 总收益率是否为正
  - 超额收益率是否长期为正
  - 超额 XIRR 是否仍为正
  - 最大回撤是否过大
  - 跑赢基准占比是否足够高
- 如果你发现某段时间突然恶化，再回到 `股票收益率`：
  - 看是哪几只票拖累
  - 打开分钟回放确认触发点
  - 必要时手动下载分钟线并重新回算

### 对产品结构的影响

- `股票收益率` 继续保留明细、分钟回放、报错日志、手动下载和回算相关操作。
- `收益率统计` 变成纯统计和纯可视化页面，适合先看全局，再钻明细。
- `AI分析报告`、`股票推荐记录` 两个栏目保持原样。

### 本次同时更新

- `CHANGELOG.md`
- `RELEASE_NOTES_1.2.7.md`
- `wails.json`
- `app_ai_api.go`
- `frontend/src/components/researchIndex.vue`
- `frontend/src/components/aiRecommendYieldStats.vue`

### 升级建议

- 升级后请重新构建前端并重启当前实例，否则浏览器可能继续使用旧静态资源。
- Web 模式下建议直接验证：
  - `研究中心` 是否出现第 4 个栏目 `收益率统计`
  - 新栏目是否能看到统计卡片
  - “全库收益走势”是否直接显示在页内
  - 原 `股票收益率` 页是否仍保留明细和操作按钮
