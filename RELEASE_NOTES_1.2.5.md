# 1.2.5

`1.2.5` 是基于 `1.2.4` 的补丁版本，重点修复“市场资讯 AI 报告已生成但推荐记录未稳定进入收益率链路”的问题，并把 AI 推荐的激活条件从自然语言收敛为可机械执行的结构化规则。

### 这次为什么要修

- 线上真实问题不是“收益率模块坏了”，而是市场资讯报告虽然已经写入 `ai_response_result`，但推荐补写阶段会因为单条记录包含模糊执行词而整批失败。
- 失败后的直接后果是：`ai_recommend_stocks` 没有新增记录，收益率链路没有输入，看起来就像“报告做了，但收益率没落库”。
- 同时，strict 收益率口径仍有若干兼容逻辑会读取 `Remarks / BuySignalDetail`，这会让模糊文本继续污染机器触发判断。

### 本版行为变化

- 推荐记录新增 `activationRuleJson` 结构化激活规则，strict 收益率模式只认该字段，不再从自然语言里猜激活条件。
- 市场资讯推荐补写改为逐条保存：
  - 合法记录正常入库并进入收益率链路
  - 非法记录单条失败并保留失败原因
  - 单条失败不再拖死整批
- 模糊执行词治理升级：
  - `放量 / 缩量 / 强势 / 承接 / 不追` 不能再直接作为激活条件
  - 必须改写成“价位 + 周期 + 比较基准 + 阈值”的量化规则
- 收益率 strict 模式会明确把缺少结构化规则的记录标记为“未结构化”，不再混入正式收益率统计。

### 对已有数据的影响

- 历史记录如果没有结构化激活规则，仍可在列表里展示，但会被标记为“未结构化”，不会进入 strict 统计结果。
- 新生成的市场资讯推荐只要能落库，就会尽量同时写入结构化激活规则；若无法生成，会明确记录失败原因。
- 备注字段继续保留给人类阅读，但不再作为 strict 模式的机器触发来源。

### 本次同时更新的发布物

- `CHANGELOG.md`
- `README.md`
- `RELEASE_NOTES_1.2.5.md`
- `wails.json` 版本号
- About 页版本徽标与公开说明文案

### 升级建议

- 升级后请重新编译二进制并重启当前运行实例，避免继续使用旧构建。
- 如果你使用 `--web` 模式，还需要重新构建前端静态资源，再启动新的后端进程。
