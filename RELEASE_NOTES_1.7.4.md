# Go-Stock 1.7.4 发布说明

1.7.4 改进股票推荐详情中的持仓期分钟图。分时和1分钟K会在每个新交易日的第一根分钟柱处绘制竖向虚线，并在价格区和成交量区保持上下对齐；分隔线会随缩放、平移和范围复位同步更新。

图表不再同时显示两个“最新”标记。工具栏新增一个全局“价格横线”开关，统一控制最新行情价、买入加权均价和卖出加权均价三条横线。横线默认隐藏，用户选择保存在当前浏览器，刷新、重启和切换其他推荐详情后继续生效。买卖图钉、区间最高最低点和交易日分隔线不受该开关影响。

本版不修改研究策略、HTTP API 或数据库结构；主库继续使用 schema 11，分钟库继续使用 schema 2。
