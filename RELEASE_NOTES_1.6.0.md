# Go-Stock 1.6.0 发布说明

1.6.0 不再延续 1.5.x 的规则策略。本版本从数据库、后端编排、调度、接口到研究中心界面，重新实现了“AI 分析 → 推荐入库 → 15 分钟生命周期判断 → 模拟成交 → 净收益”的完整链路。

## 升级说明

- 主库自动从 schema 2 升级到 schema 3，旧表和历史行不会删除。
- 旧策略触发器会被移除，1.6.0 只访问 `research_v160_*` 表。
- AI 分析默认时间为 `09:30、11:30、14:30`，默认优先选择已有 `wawa / gpt-5.6-sol / openai_responses` 配置。
- API Key 不会被复制；请在设置页确认配置有效。
- 旧推荐、旧策略收益和旧智能体会话不会混入 1.6.0 研究中心。

## 收益口径

总净收益率为：`（现金 + 持仓按可卖出净值估值 - 100,000）/ 100,000`。持仓估值会扣除预计卖出成本；卖出遵守 T+1。系统不再计算基准收益率、超额收益率或 XIRR。
