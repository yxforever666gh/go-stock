# Go-Stock 1.7.5 发布说明

1.7.5 修复持仓复查中的除权除息行情名称误判。行情名称带 `XD`、`XR` 或 `DR` 前缀，或被行情源缩写时，系统会在股票代码严格一致的前提下对名称做安全归一化；代码不同或名称无关的行情仍会被拒绝，避免串股成交。

针对本次已落库的异常记录，新增一次性、幂等且原子的受控历史卖出纠正。纠正只认可指定的推荐、观察和决策记录，按 13:30 AI 卖出决策明确引用的已落库行情补记模拟卖出，不获取、不使用后续或当前行情。13:45、14:00 和 14:15 因同一误判产生的失败重试保留在时间线中，便于完整审计。

本版不执行数据库迁移；主库继续使用 schema 11，分钟库继续使用 schema 2。
