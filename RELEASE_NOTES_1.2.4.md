# Release Notes 1.2.4

建议 GitHub Release 标题：

- `1.2.4`

建议 Release 简介：

`1.2.4` 是基于 `1.2.3` 的全量整理版本，重点收敛公开仓库内容、补齐工程结构拆分、强化推荐收益 strict 口径和分钟线回补能力，并统一当前公开版的运行时默认地址与文档说明。

## 建议正文

### 来源说明

- 当前仓库基于公开项目 `ArvinLovegood/go-stock` 改编整理而来，不是原作者官方仓库。
- 当前公开版只保留适合继续协作与二次开发的内容，并补充了公开仓库所需的文档、版本和运行时默认值收口。

### 重点更新

- 完成公开仓库整理：移除二维码、支付图、运行数据库、二进制产物、超大测试库与不适合公开的本地私有说明。
- 应用层接口继续按领域拆分，`app.go` 收敛为主结构与共享运行时逻辑，后续维护成本更低。
- 前端页面入口与重型逻辑继续下沉，路由、视图、composables 和 Wails 绑定层分层更清晰。
- AI 智能体页面升级为带历史侧栏的多轮会话模式，支持标题总结、会话持久化与轮次导航。
- 推荐收益 strict 口径继续强化，补齐上一交易日活跃度校验、分钟线回放窗口扩展与同口径 benchmark 稳定性。
- 设置页收敛到当前真实可维护能力，分钟线来源、邮件发送与 AI 配置入口更统一。
- 自动更新、基础资料同步和可选分享/资讯服务的默认地址改为当前公开仓库或显式环境变量控制，不再默认绑定原作者发布页和私有服务。

### 公开版说明

- 当前公开快照已去除个人赞赏码、联系方式、私有域名默认入口和本地工作区配置。
- 当前公开快照已移除二维码打赏、赞助码校验和赞助分流下载入口，只保留公开仓库下载路径与来源说明。
- 默认分享服务与资讯同步服务不再自动启用；如需接入，请自行通过环境变量配置。
- 如果当前仓库尚未创建 GitHub Release，应用内“检查更新”会提示无可用 Release，这是预期行为。

### 环境变量补充

- `GO_STOCK_BASEINFO_BASE_URL`
  - 覆盖基础资料同步地址，默认指向当前公开仓库的 `build/*.json`
- `GO_STOCK_SHARE_UPLOAD_URL`
  - 配置分析结果分享服务地址；未配置时，分享功能会提示改用本地导出
- `GO_STOCK_SYNC_NEWS_URL_TEMPLATE`
  - 配置资讯同步接口模板，使用 `{since}` 占位；未配置时不会主动连接外部资讯同步服务

### 升级建议

- 如果你从旧版本升级，建议先备份运行时数据库
- 如果使用 Web 模式，请在 `frontend/` 重新执行 `npm run build` 后再启动服务
- 升级后建议进入设置页复核 AI、邮件、分钟线来源和自动任务配置

### 已验证

- `go test ./...`
- `cd frontend && npm run build`
