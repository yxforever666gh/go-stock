# Go-Stock 1.7.11 发布说明

1.7.11 修复研究任务遇到 AI 中转接口间歇性断连时，多个重试在数秒内连续耗尽的问题。可重试错误现在按 1、2、4、8 秒指数退避；当两个 AI 配置实际共用同一 Base URL 时，切换配置前再冷却 8 秒。该策略不能把同一中转站变成独立容灾节点，但能让短时连接重置有机会恢复，并完整保留模型尝试日志。

市场快讯不再只读取已有数据库记录。每轮大盘研究开始前会并行刷新财联社和新浪快讯；启用 AI 自动研究时，后台每 5 分钟刷新财联社、新浪和 TradingView，并在应用启动时立即执行一次。财联社 API 失效或返回空结果时会回退到网页来源，新浪实时快讯可独立保持最近 24 小时窗口的新鲜度。

本版本不修改 AI 密钥、模型选择和数据库结构。主库保持 schema 12，分钟库保持 schema 2。
