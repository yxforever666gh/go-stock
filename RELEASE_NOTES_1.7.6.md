# Go-Stock 1.7.6 发布说明

1.7.6 将单只股票的 `50,000 元`由硬性上限调整为实际现金支出目标。系统按含滑点后的成交额、佣金和过户费计算实际扣款：普通沪深 A 股以 100 股、科创板以 200 股为最小申报单位，选择现金支出首次严格超过目标的最小整手数量。若目标数量超过可用现金，则自动回退到可承担的最大整手；只有连一手也无法承担时才跳过。

本版保留 1.7.5 对 `XD`、`XR`、`DR` 除权除息行情名称及行情源缩写的修复：股票代码仍严格匹配，名称在代码一致时归一化比较，避免误拒绝合法卖出，同时防止串股成交。

针对已落库的中际旭创推荐，受控、原子且幂等地按 2026-08-21 14:35 推荐节点的行情补记 100 股模拟买入；原“错过—资金不足”事件保留在审计时间线中。该补记不会修改 1.7.5 紫金矿业卖出纠正记录。

本版不执行数据库迁移；主库继续使用 schema 11，分钟库继续使用 schema 2。
