# Go-Stock 1.5.0 发布说明

发布日期：2026-08-05

## 发布结论

1.5.0 是一次策略与审计链路重构，不是对旧提示词的继续调参。它已进入生产推荐和模拟收益环境；当前状态明确为 **前向验证中**，标签创建不代表收益已经得到样本外证明。

V1.4.2 的历史逻辑和数据保持冻结，不回写、不重算。所有新运行使用独立的 `summary_version=1.5.0`、固定配置和配置 hash。

## 决策逻辑

- 510300 决定 `risk_on / neutral / risk_off` 市场状态。基准陈旧时按 neutral 降级并告警，不全局停产。
- 股票必须通过 ST、上市天数、停牌、成交额、价格、ATR 和价格数据完整性硬门槛，并通过涨幅、跳空与偏离 MA20 的追涨约束。
- 全部候选先做确定性评分和排名，再取前 18 只做事件证据核验；总分至少 70 且硬门槛全部通过才允许生产。
- 候选池不再沿用旧版 120 条截断或“近期失败/凑数量”惩罚；证据核验完成后重新确定性排序，并在最终占用名额前刷新报价和资格。单只报价失败或过期只拒绝该候选并告警。
- LLM 不再决定股票顺序、生产评分、目标价或执行状态。新闻为空或失败时事件分为 0，技术链路仍可继续。
- 每个交易日共享新增名额，三次定时运行不会重复计算额度；同板块当日最多一只，同股待激活或持仓期间不能重复，止损后冷却 5 个交易日。

## 交易与收益口径

- 只支持 `pullback` 与严格 `breakout` 两条路径，均要求完整 15 分钟 bar 确认，并在下一根 bar 才能成交。
- 激活有效期为 3 个交易日；持有最长 10 个交易日。止损距离为 `1.5 ATR` 与 60 日隔夜负跳空风险的较大值，并限制在 3%–6%。
- 初始目标为 2R；近期阻力只作为目标上限，若上限后的可实现收益低于 1.5R，则拒绝交易。
- 达到 +1R 后使用“最高收盘价 - 1.5 ATR”移动止损；同一 bar 同时触及止盈和止损时按止损优先。
- 模拟本金 100,000 元，单笔目标 10,000 元；数量按 `floor(10000 / (成交价 × 100)) × 100` 计算，不足一手标记为 `untradeable_min_lot`。
- 手续费基于实际成交数量和名义金额；报告同时保存每侧 10bp 基准滑点与 20bp、50bp 压力场景。
- 510300 基准采用与 V1.5.0 组合一致的 100 股整手、每侧 10bp 滑点和实际费用口径；不足一手的现金保留在基准账户中，不再按小数份额或旧版 5bp 口径比较。
- 独立执行监控器按完整 15 分钟 bar 加 45 秒数据宽限运行，不依赖前端页面；重启后可从不可变事件账本恢复，单股行情观察失败会保留为可重试告警，不会误写永久拒绝。
- 公司行为使用 Tushare 分红送转和复权因子的当日点时覆盖，并以 `ok / empty / failed` 冻结；在除权日首根 bar 前同步调整待激活计划、持仓数量及价格，并把税后现金分红记入订单事件账本。覆盖缺失或迟到、无法解释的复权因子和未定义参与规则的配股只拒绝对应股票，不影响同批其他标的。
- V1.5.0 组合和 510300 都从 100,000 元初始权益计算净值。陈旧现价不会被新时间戳包装，也不会删除既有历史点；缺价日明确省略并告警，不生成零盈亏的伪完整数据。

## 数据、审计与回放

新增下列表并采用 append-only / immutable 语义：

- `strategy_run_snapshot`
- `strategy_candidate_snapshot`
- `strategy_rule_snapshot`
- `strategy_order_event`
- `strategy_backtest_run`
- `strategy_backtest_trade`
- `strategy_backtest_metric`
- `security_master_history`
- `corporate_action_event`

每轮运行保存所有候选的点时特征、阶段排名、证据时间、淘汰原因和相关 hash。无合法候选时保存结构化 `no_trade`，不会再表现为模型空响应。

每日共享名额和最多 5 个持仓均从不可变候选、规则和订单事件账本重建；最终发布在同一事务内重新检查额度，并排除当前 run 的自身规则，避免并发超额或孤儿状态。执行回放仍使用严格点时可见性，发布计数则包含所有已经提交的不可变事实。

缓存回放命令：

```shell
go run . strategy-backtest --version 1.5.0 --from 2026-08-01 --to 2026-10-31
```

该命令只读取本地冻结快照，不会联网补行情、新闻或公司行为数据；输入不完整时直接失败并报告缺口。

冻结历史规则的执行正确性审计命令：

```shell
go run . strategy-rule-replay --to 2026-08-04 --expect-count 226
```

该命令以 SQLite `mode=ro` 读取本地缓存，并以 `2026-08-04` 为冻结边界，对计划基线中的 226 条结构化旧规则执行两遍资格、激活、T+1 与退出路径。在缓存时钟 `2026-08-05 14:37 +08:00` 的发布审计中，226/226 可解析、因果违规 0、T+1 违规 0，两遍 hash 均为 `2de5273b56153368183d8fae919f3397ae7a4d635e99167a8ea978b1c339ba1d`。它只证明同一冻结输入下执行层可重复，不是 V1.5.0 选股收益证明；历史输入中的 1 条分钟缓存缺口与 7 条缺少退出边界记录保持冻结并明确报告。发布验收期间旧进程在 `2026-08-05` 新增的 1 条 1.4.2 规则另行只读回放，解析、因果、T+1 与双遍确定性同样通过，未被删除或回写。

事件回放对已平仓头寸按实际退出事件计价；期末未平仓头寸仅按最后事件价或入场成本估值，并在报告中标明 `event_price_or_entry_cost`，不冒充实时盯市价格。

## 验证边界

上线后的收益验证至少收集：

- 60 个交易日；
- 100 笔平仓；
- 40 个独立推荐日。

转为“已验证”还需同时满足：净均值不低于 0.75%、相对 510300 净超额不低于 0.50%、Profit Factor 不低于 1.25，以及按推荐日分组的单侧 90% 置信下界大于 0。

在满足这些条件前，界面持续显示“前向验证中”。任何后续参数变化必须进入新版本，不能静默修改 1.5.0。

## 运行范围

“生产”仅指生产推荐和虚拟账户收益跟踪。仓库中不存在券商自动下单接口，本版本不会发送真实证券订单。
