# Go-Stock 1.6.7 发布说明

1.6.7 将项目正式收敛为仅本机访问的 Web 应用。Wails 桌面运行时、生成绑定、反射 RPC 和双桥已退出工程，前后端统一通过显式 `/api/v1` HTTP API 与 WebSocket 事件通信。

市场行情全部 12 个页签、股票自选、普通诊股、基金、设置、情感分析、研究中心、定时研究和生命周期任务保持不变。主库 schema 9、分钟库 schema 2，本版本不修改数据库结构。

运行方式：直接启动 `go-stock-web.exe`，然后访问 `http://127.0.0.1:34115`。旧脚本传入的 `--web` 参数在 1.6.x 内继续作为兼容参数接受。
